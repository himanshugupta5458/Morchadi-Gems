# Build Log

Running record of what each prompt built. Append one row per prompt; never rewrite past
rows. Conventions are in [README.md](README.md).

| Prompt # | Date | What was built | Docs updated | Status |
| --- | --- | --- | --- | --- |
| 1 | 2026-08-17 | Project foundation. Scaffolded Next.js 14 App Router + TypeScript strict + Tailwind (`create-next-app@14.2.35`, no `src/`, `@/*` alias). Authored `CLAUDE.md` with project overview, autonomy directive, coding conventions, and the mandatory docs rule. Created the full `/docs` hierarchy with a README per folder. Created the app folder structure (`app/`, `components/`, `lib/`, `data/`, `types/`). Added `.env.example` covering `CASHFREE_APP_ID`, `CASHFREE_SECRET_KEY`, `CASHFREE_ENV`, `NEXT_PUBLIC_BASE_URL`. Added `npm run typecheck`, renamed the package to `morchadi-gems`, hardened `.gitignore` against `.env`, and replaced the boilerplate home page with a placeholder. Verified `npm run build`, `npm run typecheck`, and `npm run dev` (HTTP 200 on `/`). | `CLAUDE.md`, `docs/README.md`, `docs/decisions/README.md`, `docs/decisions/ADR-001-tech-stack.md`, `docs/api/README.md`, `docs/progress/README.md`, `docs/progress/BUILD_LOG.md`, `docs/logs/README.md`, `docs/testing/README.md` | Complete |
| 2 | 2026-08-17 | Data layer, no UI. `types/product.ts` (`Category` union of 8 slugs, `CATEGORIES` slug/label table plus `getCategoryLabel` and `isCategory` guards, `Review`, `ProductDetails`, `Product`) and `types/cart.ts` (`CartItem`, `Address`, `CheckoutData`). `data/products.json` with 100 products across all 8 categories (12–13 each), per-category id prefixes, 8 featured, 8 isNew, 3 out of stock, prices spread 27 budget / 43 mid / 30 premium. `lib/products.ts` exposing the six accessors as the only path to product data. `scripts/validate-products.mjs` plus `npm run validate:products`, covering 22 shape and convention checks, all green, with 5 injected-fault cases confirmed to fail loudly. Notable choice: the seed catalogue was produced by a curated generator run once from a scratch directory and then discarded rather than committed, so `data/products.json` stays the single source of truth — reasoning in ADR-002. | `docs/decisions/ADR-002-product-data-model.md`, `docs/decisions/README.md`, `docs/testing/PLAN-product-catalogue.md`, `docs/testing/RESULT-2026-08-17-product-catalogue.md`, `docs/testing/README.md`, `docs/progress/BUILD_LOG.md` | Complete |
| 3 | 2026-08-17 | Design system foundation — atoms only, no pages. **Data:** added `mrp` (display-only compare-at price) to the `Product` interface and to all 100 products, seeded 10–40% above `price` and rounded to retail-plausible figures; 95 discounted, 5 at full price, highest implied discount 28.7%. Extended `scripts/validate-products.mjs` with three `mrp` checks (positive whole number, `>= price`, implied discount ≤ 60%); 100 pass and six injected faults confirmed to exit 1. **Tokens:** Fraunces (display, incl. italic) and Jost (sans) via `next/font/google` as `--font-display` / `--font-sans`; 11 colour tokens, a 10-step type scale, tracking, container, radius, shadow and duration tokens in `tailwind.config.ts`; base typography in `globals.css`. Removed the unused Geist local fonts. **Components:** `SectionHeading`, `Button` (the sole Client Component), `StarRating`, `ProductCard`, `TrustBadge`, `TrustStrip`, `icons.tsx`, plus `lib/format.ts`. **QA:** `/style-guide` renders every primitive against real catalogue data, `noindex` and unlinked. Notable fixes: generated the missing `public/placeholder-product.png` — ADR-002's `/placeholder-product.jpg` was never created, so every card pointed at a 404 — and repointed the catalogue at it; corrected a `0% off` chip on full-price products in the style guide's price panel. Not visually verified: no browser is available in this environment, so `/style-guide` was checked by asserting on the rendered HTML (all four card states, star fills, chips, trust badges, image optimisation) rather than by screenshot. | `docs/decisions/ADR-003-discount-display-pricing.md`, `docs/decisions/ADR-004-design-system.md`, `docs/decisions/README.md`, `docs/design/README.md`, `docs/design/DESIGN_SYSTEM.md`, `docs/README.md`, `docs/progress/BUILD_LOG.md`, `CLAUDE.md` | Complete |
| 4 | 2026-08-17 | Global chrome plus three design-QA refinements. **QA fixes:** `ProductCard` lost the inner frame around the image area (the `border-t` divider between image and body) and its image ground moved white → `ivory`; elevation standardised so every card rests flat on the hairline border with the lift (`-translate-y-1` + `shadow-card-hover`) on hover only. `SectionHeading` gained a `tone` prop — `light` (default, gold accent) and `honey` (maroon accent and rule, for the honey band where gold on gold goes illegible); `/style-guide` shows both on their real grounds. **Chrome:** `AnnouncementBar` (charcoal strip, 3 messages cross-fading every 4s, all three kept in the a11y tree, fade dropped under `prefers-reduced-motion`); sticky `Header` composing `Wordmark`, `CartLink`, `CategoryNavBar`, and `MobileNav`; a flat-category mega-nav where each of the 8 categories opens a panel of four `/shop` quick filters — All + three price bands — with `aria-expanded`/`aria-controls`, hover/click/ArrowDown open, Escape-to-close returning focus, and `ring-offset-charcoal` focus rings; a mobile drawer with focus trap, scroll lock, and an 8-category accordion carrying the same four links; charcoal `Footer` (brand blurb, 8 category links, About/Contact/Terms, Cashfree line, copyright); floating `WhatsAppButton` off `lib/config.ts`. `lib/navigation.ts` derives `NAV_CATEGORIES` from `CATEGORIES` so nav, drawer, and footer share one source. **Cart seam:** `lib/cart-count.ts` exports `useCartItemCount()` returning 0; the badge hides at 0 and wiring real cart state later touches only that hook — no partial cart store was built. **Testimonials:** `types/testimonial.ts`, `data/testimonials.json` (6 store-level, 4–5 star), `lib/testimonials.ts`, `TestimonialCard` (monogram avatar from `getInitials`, no photos), and `TestimonialBand` + `TestimonialCarousel` — snap carousel with dot pagination below `lg`, 3-col grid above, scroll position as the single source of truth for the active dot. Wired into `app/layout.tsx` (which now owns the single `<main>`; `/style-guide` changed from `<main>` to `<div>` to avoid nesting) and the band rendered on the placeholder home for QA. Judgement call: testimonial stars stay `amber` rather than gold, since `amber` is the reserved rating colour and two star scales on one page would read as two different ratings. Deferred: the real WhatsApp number — `910000000000` is a placeholder in `lib/config.ts`. Not visually verified: no browser in this environment, so both routes were checked by asserting on rendered HTML. | `docs/decisions/ADR-005-navigation-and-chrome.md`, `docs/decisions/README.md`, `docs/design/DESIGN_SYSTEM.md`, `docs/progress/BUILD_LOG.md` | Complete |
| 5 | 2026-08-17 | Dummy product-image system, structured so real photography drops in with zero code or data changes. **Convention:** image paths are now derived from the id rather than stored — `/products/{id}.webp` per product and `/categories/{slug}.webp` per category. All 100 products repointed from the shared `/placeholder-product.png` to their own path; `images` stays a `string[]`. Replacing a placeholder is a file drop at the same path — nothing to edit, nothing to rebuild. **Generator:** `scripts/generate-placeholders.mjs` + `npm run generate:placeholders`, using `sharp` (devDependency, never runs on Vercel). Writes only where no file exists — no `--force`, deliberately, since the thing at risk is photography that may exist nowhere else. Produced 100 product placeholders (1000×1000) and 8 category tiles (1200×1500): ivory field with a per-category tint, the gold gem motif from `icons.tsx`, gold rule, uppercase category eyebrow, and the product name wrapped to at most 2 lines. Output committed (~1.3 MB) so placeholder and real photo share one storage location. Brand fonts not embedded — DejaVu Serif/Sans via the rasterizer, per the prompt's graceful-fallback allowance. **Two design calls:** the tint fades *outward* to ivory rather than inward, because `ProductCard` insets the image inside an ivory area and an edge-tinted field would redraw the inner frame prompt 4's QA removed (verified by compositing a card simulation); and the placeholders carry no "coming soon" text, since 100 cards announcing their own incompleteness defeats the "render populated and real-looking" goal — `ProductCard`'s in-component "Image coming soon" fallback is retained but scoped to an empty `images[]`, which is a data error. **Validation:** `validate-products.mjs` now asserts `images[0]` is exactly `/products/{id}.webp` *and* that the file exists on disk, plus that all 8 category files exist, with an images section in the summary. Four injected faults confirmed to exit 1 (wrong path, wrong extension, missing product file, missing category file). Idempotency and non-clobbering verified: a re-run reports 100/8 skipped, and a 1400×1400 stand-in planted at `nk-001.webp` survived a re-run byte-identical. Deleted the now-orphaned `public/placeholder-product.png` (ADR-004's asset, superseded). No `next.config` `remotePatterns` needed — everything is local under `/public`. Sample assets confirmed HTTP 200 raw and through `/_next/image`. | `docs/decisions/ADR-006-product-image-convention.md`, `docs/decisions/README.md`, `docs/design/IMAGES.md`, `docs/design/README.md`, `docs/design/DESIGN_SYSTEM.md`, `docs/progress/BUILD_LOG.md` | Complete |
| 6 | 2026-08-17 | The real Home page, composing the design system into the storefront showcase. Replaced the placeholder `app/page.tsx` with six sections in order: Hero → Shop by Category → New Arrivals → Best Sellers → The Morchadi Promise → Customer Speak. Chrome inherited from `layout.tsx` untouched. **Hero:** typographic and brand-led — eyebrow, two-tone display headline ("THE EVERYDAY" roman + *Heirloom* gold italic), gold rule, positioning line, `Shop Collection` → `/shop` and `Explore Categories` → `#shop-by-category`, plus a catalogue count. The composition reads finished with the image removed; the 4:3 side panel at `/hero/home-hero.webp` is support, not structure. **Reusable pieces:** `ProductGrid` (2/3/4 columns, page-agnostic — takes `products`, optional `onAddToCart`, `priorityCount`; built for Shop reuse and used twice on Home the day it was written), `CategoryTile` + `CategoryGrid` (8 portrait tiles off `CATEGORIES`, bottom scrim, hover zoom, whole tile links to `/shop?category={slug}`), `ViewAllLink`, `Hero`, and `ButtonLink` — an `<a>` twin of `Button`, both now reading `buttonClasses()` from the new `lib/button-styles.ts` so a variant cannot drift between button and link. Added `ArrowRightIcon` and `buildCategoryImageSrc()`. **Generator:** extended `scripts/generate-placeholders.mjs` with a `/hero/home-hero.webp` slot (1600×1200), same idempotent skip-if-exists rule; wrote 1, and the tally line now reports Hero alongside Products and Categories. Deliberately wordless — the headline beside it already carries the brand name, and the hero `alt` is descriptive so a real photo swap stays a pure file drop. **SEO:** `metadataBase` from `NEXT_PUBLIC_BASE_URL`, `"%s · Morchadi Gems"` title template, default description and full OpenGraph block in `layout.tsx`, shared strings moved into `SITE_CONFIG`; Home sets its own title via `absolute`, canonical `/`, and a complete OG block. Fixed `/style-guide`'s title, which was double-branding under the new template. **Caught during verification:** the first version of Home set a partial `openGraph` and silently lost `og:type`, `og:site_name`, `og:locale` and `og:image` — a page's `openGraph` replaces the layout's rather than merging. Found by inspecting served HTML, not by reading docs; noted in ADR-007 for every later page. Also added `scroll-behavior: smooth` with a `prefers-reduced-motion` opt-out for the hero's anchor CTA. `/style-guide` gained panels for `ButtonLink`, `ViewAllLink`, `ProductGrid` and `CategoryTile`. Home is fully server-rendered — no section added here is a Client Component. Pending routes that 404: `/shop` (+ `?category=`, `?sort=newest`, `?sort=rating-desc`), `/product/[id]`, `/cart`, `/about`, `/contact`, `/terms`. Not visually verified: no browser in this environment, so the page was checked by asserting on rendered HTML (section order, 8 tiles, 16 products, both view-all links, hero preload, OG tags). | `docs/decisions/ADR-007-home-composition.md`, `docs/decisions/README.md`, `docs/design/DESIGN_SYSTEM.md`, `docs/design/IMAGES.md`, `docs/progress/BUILD_LOG.md` | Complete |
| 7 | 2026-08-17 | Shop page — full catalogue with URL-driven filtering, sorting and pagination — plus the project's first unit-test runner. **Logic core:** `lib/shop-query.ts` owns the URL vocabulary (`PRICE_BANDS` with inclusive bounds, `SORT_OPTIONS`, `DEFAULT_SORT`, `PER_PAGE = 12`, `parseShopQuery`, canonical `buildShopHref`, query mutators, `buildPaginationRange`) and imports no product data; `lib/shop.ts` adds the pure `getShopResults` and re-exports it. The split is load-bearing: the filter controls are Client Components that need `buildShopHref`, and had it lived beside `getShopResults` every visitor would have downloaded all 100 products. Verified post-build — no client chunk contains catalogue data, and `/shop` costs 1 kB more first-load JS than `/`. **Testing (SDLC milestone):** added Vitest (`npm test`, `npm run test:run`, config in `vitest.config.mts`) and `lib/shop.test.ts` — **53 tests, all passing**, covering band edge inclusivity at 999/1000/4999/5000, all four sorts across every page, tie-breaking, facet OR/AND semantics, an empty combination, invalid category/price/sort, page clamping (0, -1, 9999, non-numeric, empty, fractional), purity, href round-trip, and the pagination range at all 20 positions. `scripts/validate-products.mjs` untouched and still green — data validation and logic tests coexist. **UI:** `app/shop/page.tsx` (server) with a category-aware heading, "Showing X–Y of Z pieces", removable filter chips + Clear all, the reused `ProductGrid`, a tasteful empty state, and numbered `Pagination`; Client Components `ShopFilterPanel`, `ShopFilterDrawer` (mobile slide-over, focus trap, scroll lock, Escape) and `ShopSortSelect`. Added `FilterIcon`. **Decisions (ADR-008):** URL is the single source of truth with no local filter state to desync; facets are multi-select comma-separated, OR within / AND across, because checkboxes that deselect each other are a lie about the control — single-category nav links are just the one-element case; price bands over a free min/max range; default sort `newest` = `isNew` first then rating, since the catalogue has no timestamp; every comparator ends on `id` so pagination cannot drop or repeat a product; out-of-range pages clamp rather than 404. **SEO:** `generateMetadata` with category-aware title/description, canonical that follows the *clamped* page (`?page=9999` → `?page=9`), and the full OG block per ADR-007's replace-not-merge lesson. **Two defects caught during verification:** a tie test that asserted against the catalogue's cheapest price, which happens to be unique — a bad test failing a correct implementation, fixed to locate a real tie; and a generic shop meta description that double-mentioned the brand, caught by reading the served `<meta>` tag since no test covers copy quality. `/shop` is `ƒ (Dynamic)` — inherent to reading `searchParams`. All 43 inbound `/shop` links from the chrome and home verified 200. Still 404 pending later prompts: `/product/[id]`, `/cart`, `/about`, `/contact`, `/terms`. Not visually verified: no browser in this environment. | `docs/decisions/ADR-008-shop-architecture.md`, `docs/decisions/README.md`, `docs/testing/PLAN-shop-logic.md`, `docs/testing/RESULT-2026-08-17-shop-logic.md`, `docs/testing/README.md`, `docs/design/DESIGN_SYSTEM.md`, `docs/progress/BUILD_LOG.md` | Complete |
| 8 | 2026-08-17 | Product detail page for all 100 products, statically generated. **Route:** `app/product/[id]/page.tsx` with `generateStaticParams` over the full catalogue and `dynamicParams = false` — the catalogue ships as code, so an id outside the build-time set is always wrong and 404s without invoking the page. Verified by counting prerendered HTML under `.next/server/app/product` and diffing against catalogue ids: 100 present, none missing, none extra; all 100 URLs returned 200 and invalid/wrong-case/near-miss ids returned 404. Added a root `app/not-found.tsx` — on-brand, wrapped in chrome, "Back to Shop" + "Go to homepage" — which now also improves the 404 for `/cart`, `/about`, `/contact`, `/terms`. **Page:** breadcrumb (Home / Shop / Category → `/shop?category={slug}` / name), image panel, category eyebrow, display-serif h1, StarRating anchor-linking to `#reviews`, `PriceDisplay` at `lg`, short description, quantity stepper, Add to Cart + Buy Now, details definition list rendering only the keys present (verified: a closure-only and a type-only product each render three rows, no empty row), reviews section, and a related `ProductGrid` of 4. **Shared extractions, with existing call sites retrofitted:** `PriceDisplay` (the single implementation of ADR-003's rules — `ProductCard` *and* the `/style-guide` price panel, which had been hand-rolling the same markup, now both use it), `Monogram` (`TestimonialCard` + `ProductReviews`), `ProductImagePlaceholder` (`ProductCard` + `ProductImagePanel`). **Gallery built but dormant:** `ProductGallery` (client) is only rendered when `images.length > 1`; every product has exactly one, so the page renders the server `ProductImagePanel` and ships **no client JS for images**, and there is deliberately **no thumbnail row** for a single image — a one-thumbnail strip is decoration pretending to be a control. The dormant branch is rendered on `/style-guide` against a synthetic two-image product so it is not shipped unseen. **Quantity:** `lib/quantity.ts` `clampQuantity` (1–10, integer) is the only definition of a valid quantity — buttons, typing and paste all route through it — with 6 unit tests; suite now **59 passing across 2 files**. **Purchase buttons are inert by design:** `ProductPurchasePanel` takes optional `onAddToCart`/`onBuyNow` matching `ProductCard`, and a Server Component cannot pass functions, so neither is supplied. No cart state was built. `onBuyNow`'s intended behaviour (add, then navigate to `/address`) is recorded in ADR-009 since it cannot be inferred from the code. **Out of stock:** stepper and both buttons disabled (5 disabled controls), primary relabelled "Sold out", explanatory line added — details, reviews and related all still render. **SEO:** per-product `generateMetadata` — title via the layout template, description from `shortDescription`, canonical, and a full 12-tag OG block including the 1000×1000 product image, per ADR-007's replace-not-merge lesson. **Notable fix:** a regex-driven retrofit of the style guide over-matched and destroyed the `ProductCard` and `TrustBadge`/`TrustStrip` panels; caught by typecheck (`JSX expressions must have one parent element`) and both panels restored and re-verified in the served HTML. Build: 107 prerendered pages, only `/shop` remains dynamic. Still 404 pending later prompts: `/cart`, `/address`, `/about`, `/contact`, `/terms`. Not visually verified: no browser in this environment. | `docs/decisions/ADR-009-product-page.md`, `docs/decisions/README.md`, `docs/design/DESIGN_SYSTEM.md`, `docs/testing/README.md`, `docs/progress/BUILD_LOG.md` | Complete |
| 9 | 2026-08-17 | The cart layer — global state, the `/cart` page, and every add-to-cart stub left across the site made real. **Pure core first:** `lib/cart.ts` holds all cart arithmetic as pure functions over arrays — `addProductToCart` (merges into the existing line, never duplicates, clamps), `removeProductFromCart`, `setCartItemQuantity`, `reconcileCartWithCatalogue`, `parsePersistedCart`, `buildCartLines`, `calculateCartTotals` — so the money is testable without React and reusable by the checkout route later. `clampQuantity` from prompt 8 stays the only definition of a valid quantity. `FLAT_SHIPPING_RATE = 99` moved into `lib/config.ts`; `mrp` does not appear in `lib/cart.ts` at all. **State:** `CartProvider` (`lib/cart-context.tsx`) mounted in `app/layout.tsx`, persisting to `localStorage` under `morchadi-cart-v1`, exposing `useCart()` with `items`, `lines`, `addItem`, `removeItem`, `setQty`, `clearCart`, `itemCount`, `subtotal`, `shipping`, `total`, `hasUnavailableItems`, `isHydrated`. **The catalogue is the source of truth for money:** `CartItem` keeps its price snapshot for legibility but nothing reads it — lines price off the catalogue entry, and reconciliation overwrites the snapshot on every hydrate, so a tampered `localStorage` price changes nothing. **Client payload:** `CartProvider` is a Client Component and could not import `lib/products.ts` without dragging `data/products.json` into the bundle, so `getCatalogueIndex()` projects the catalogue to a new lean `CatalogueEntry` type (id, name, price, mrp, image, inStock) which `app/layout.tsx` passes in as a prop — 11.9 kB instead of 67.9 kB of raw JSON, measured at 14.3 kB raw / 2.2 kB gzipped of flight payload on `/product/nk-001`, with First Load JS unchanged at ~106 kB and no `shortDescription`/`reviewCount`/`reviews` field in any served page. `AddToCartButton` and `ProductPurchasePanel` take a `CatalogueEntry` rather than a `Product` for the same reason. **Hydration:** initial state is `[]` on the server *and* on the first client render; a `useEffect` then reads storage, reconciles, and flips `isHydrated`. The badge is absent from both renders and appears in the commit after hydration — a state update, not a mismatch. `/cart` shows a loading line until `isHydrated` rather than flashing the empty state. **Stubs wired:** `ProductCard` lost its `onAddToCart` prop and now slots in the `AddToCartButton` island, so card grids stay Server Components (`ProductGrid` lost the prop too); `ProductPurchaseActions` wraps `ProductPurchasePanel` on the product page — Add to cart adds and toasts, Buy now adds then `router.push('/address')`; `CartLink` reads live `itemCount`. `lib/cart-count.ts` and its `useCartItemCount()` seam were **deleted** rather than left as an alias — the badge reads `useCart()` directly. **Cart page:** `app/cart/page.tsx` is a Server Component owning the metadata (`noindex, follow`) around a client `CartView`, which picks loading / empty / populated. Line items carry thumbnail, name linking to the product, unit `PriceDisplay`, `QuantityStepper`, line total and remove; an item that went `inStock: false` is kept, desaturated, marked "Out of stock", excluded from totals, and blocks checkout until removed — a cart of only sold-out lines totals ₹0, not ₹99. Sticky order summary from `lg`; on-brand empty state. **New shared UI:** `ToastProvider` / `useToast` / `ToastViewport` (one toast at a time, always-mounted `role="status"` live region, bottom-left to clear the WhatsApp button), a `toast-in` keyframe disabled under `prefers-reduced-motion`, and `CheckIcon`. `QuantityStepper` gained `accessibleLabel` and `useId` so several steppers on `/cart` do not share a DOM id or an ambiguous label. `CHECKOUT_ADDRESS_PATH` and `CART_PATH` centralised in `lib/navigation.ts`, reusing the existing `SHOP_PATH` rather than adding a second one. **Testing:** `lib/cart.test.ts` (49 cases) plus `lib/cart-context.test.tsx` (10 cases) — the first jsdom suite in the project, so `vitest.config.mts` now picks up `.test.tsx` with `oxc.jsx: automatic` and per-file `@vitest-environment` docblocks, and `jsdom` + `@testing-library/react` joined the devDependencies. Suite is **118 passing across 4 files**. Two adversarial checks were run and are recorded honestly: a throwaway probe proved `console.error` really does fire on a hydration mismatch (so the four hydration assertions are not vacuous), and injecting the rejected `useState`-initialiser-reads-`localStorage` implementation made the server-render case fail — though under jsdom it did *not* trip the mismatch assertions, since `renderToString` there also sees a `window`. **Not visually verified:** no browser in this environment; everything was asserted on served HTML or a jsdom tree, so layout, the sticky summary and the toast animation have not been looked at. **Known gap, deliberately deferred:** no cross-tab `storage` sync — two open tabs overwrite each other's cart. Still 404 pending later prompts: `/address`, `/about`, `/contact`, `/terms`. | `docs/decisions/ADR-010-cart-architecture.md`, `docs/decisions/README.md`, `docs/testing/PLAN-cart-logic.md`, `docs/testing/RESULT-2026-08-17-cart-logic.md`, `docs/testing/README.md`, `docs/design/DESIGN_SYSTEM.md`, `docs/progress/BUILD_LOG.md` | Complete |
| 10 | 2026-08-17 | Checkout step 1 — the `/address` page: guest delivery details, validated, and bridged to `/payment`. **Validation as pure functions:** `lib/address.ts` holds one validator per field returning `string | undefined`, plus `validateAddressForm` which runs all of them and returns `{ errors, address }` — `address` non-null exactly when `errors` is empty, and trimmed and normalised, so the caller cannot store raw form input and there is no cast between checked and typed. It deliberately does not stop at the first failure: one submit reports every problem. No validation library — eight fixed Indian-address rules with brand-voice copy did not justify a dependency and a DSL. **One states list:** `INDIAN_STATES` (28 states + 8 UTs, `as const`), the derived `IndianState` union and `isIndianState` live in `types/cart.ts` beside `Address`, mirroring how `types/product.ts` holds `CATEGORIES`/`Category`. The dropdown maps over it and the validator checks against it, so the control cannot offer a value the validator rejects; `Address.state` is typed `IndianState`, not `string`. Verified in the served build: the `<select>` renders exactly 37 options — 36 states plus a `""` placeholder. **The sessionStorage bridge, and what it is not:** on a valid submit `buildCheckoutData` assembles a `CheckoutData` and writes it under `morchadi-checkout-v1` so a summary survives the coming Cashfree redirect. ADR-011 states bluntly that these amounts are **display state only** — the order-creation route will recompute every one from the bundle's ids and quantities against `data/products.json` and ignore the stored numbers. `parseCheckoutData` validates *shape* only, and a test asserts that a tampered `total: 1` **passes**, because rejecting it would imply this layer was ever an authority on money. The bundle is checked to contain no `mrp` anywhere. `sessionStorage` over `localStorage` so a half-finished checkout dies with the tab. **Graceful degradation:** `writeCheckoutData` catches quota and private-mode failures, returns `false`, and the page navigates anyway — a checkout must not die because a summary could not be cached. The consequence is handed forward and recorded: **`/payment` must treat a missing bundle as normal** and send the shopper back. **Repopulation:** the bundle is read on mount and seeds the form via `toAddressFormValues`, so `/payment`'s Edit affordance will be a real back-navigation rather than a retype; a corrupt bundle is ignored and the form starts empty. **Guard, not redirect:** an empty cart or a sold-out line renders `CheckoutGuardNotice` — an explanation plus Back to cart / Continue shopping. A redirect would have to fire from an effect that races the cart's hydration, and a checkout that silently relocates you reads as a fault. **No flash:** nothing is decided until `isHydrated` *and* the sessionStorage read have both happened. Confirmed against the served build — the prerendered `/address` HTML contains the loading notice once and zero occurrences of the guard text, "Delivery details", "Full name" or "Order summary". **Form behaviour:** `noValidate` plus `preventDefault`, so no native navigation and no browser validation bubbles; validation on blur, re-validation while typing only for a field already flagged, everything on submit; focus moves to the topmost invalid field via `ADDRESS_FIELDS` order. Submit is deliberately **not** disabled while invalid — a dead button explains nothing. **Shared UI added:** `FormField` (label, control slot, error line, and the aria wiring in one place — `aria-invalid`, `aria-describedby` at `${id}-error`, `sale` border), `TextField`, `SelectField`, `FormFieldPreview`, `CheckoutSteps`, `CheckoutSummary`, `CheckoutGuardNotice`, `PanelNotice`, and `OrderTotals` — extracted from `CartSummary` so the cart and every checkout step render one total the same way. `CartView`'s bespoke loading line was replaced by `PanelNotice`. `CHECKOUT_PAYMENT_PATH` added to `lib/navigation.ts`. **Testing:** `lib/address.test.ts` (50), `lib/checkout.test.ts` (17), `lib/address-checkout.test.tsx` (16) — suite now **201 passing across 7 files**. **The adversarial check changed the tests:** deleting the hydration wait — the implementation ADR-011 rejects — produced **no hydration mismatch at all**, because the server render and the first client render still agree; the bug is the second render swapping the guard for the form, a flash of wrong content React has no reason to warn about. The prompt-9 mismatch assertions would have missed it, and the no-flash case as first written checked only the post-hydration DOM, which the faulted build also satisfied. It was strengthened to assert on the server HTML; the strengthened version fails against the fault, the original did not. **Two copy/API fixes found by testing:** the empty-state error for the state field read "Select a state", identical to the dropdown placeholder — merged with the invalid-state message to "Select a state from the list", since for a `<select>` those are the same mistake; and a stray escaped quote in a new `/style-guide` panel note broke the file's JSX, caught by typecheck and confirmed fixed by extracting all 23 panel headings from the served HTML (prompt 8's regex-retrofit incident). **Not visually verified:** no browser in this environment; field spacing, the sticky summary and the errored focus ring have not been looked at, and `autoComplete` has not been exercised against real browser autofill. Still 404 pending later prompts: `/payment` (now reachable from two live CTAs), `/about`, `/contact`, `/terms`. | `docs/decisions/ADR-011-checkout-address-step.md`, `docs/decisions/README.md`, `docs/testing/PLAN-address-validation.md`, `docs/testing/RESULT-2026-08-17-address-validation.md`, `docs/testing/README.md`, `docs/design/DESIGN_SYSTEM.md`, `docs/progress/BUILD_LOG.md` | Complete |
| 11 | 2026-08-17 | Static content and the full policy set — the six routes that were still 404ing. **Why the policies ship now:** Cashfree will not activate a live merchant account without published terms, privacy, refund and shipping policies, so they are a gating requirement for taking money rather than a copy task for later. **Consistency enforced by construction, not by proofreading:** `RETURN_WINDOW_DAYS` joined `FLAT_SHIPPING_RATE` in `lib/config.ts`, and `TrustStrip` now reads *both* instead of its hardcoded "Flat ₹99 Shipping" / "Easy 7-Day Returns" labels — so the promise on the home page and the promise in the refund policy are the same expression. Verified against the served build: `7` is the only day-number appearing anywhere on `/`, `/about`, `/cart` or the four policies, and every shipping mention is ₹99. The prose matches the non-numeric mechanics too — INR tax-inclusive with no GST line, Cashfree hosted redirect with no card data on our side, no accounts, `localStorage` cart and `sessionStorage` checkout, India-only shipping. **Sample-template disclaimer:** `PolicyDisclaimer` sits above the content on all four policies as a *component*, so it cannot be dropped from one page and left on the others. Unresolvable business facts are marked rather than invented — `[REGISTERED ENTITY NAME]`, `[CITY]`, `[STATE]`, and `[PLACEHOLDER — confirm]` on the non-returnable categories, which is a merchandising call that cannot be made from inside the repo. A guessed jurisdiction city would look finished and be wrong. **Shared shell:** `PolicyPage` owns the breadcrumb, two-tone heading, `Last updated` `<time>`, disclaimer, prose wrapper and sibling cross-links (from a new `POLICY_LINKS` filtered by current path), so a policy page contains only its own words. `Prose` styles descendants by element via arbitrary variants — plain `<h2>`/`<p>`/`<ul>` land on the type scale with gold list markers — deliberately **not** `@tailwindcss/typography`, whose opinionated stylesheet would have to be overridden back to our tokens. **Contact form, one handler and one branch, keyed on `NEXT_PUBLIC_WEB3FORMS_KEY`** (a public submission token identifying an inbox, not a secret — hence no API route). Key present: POST to Web3Forms, with a non-OK response or a network failure rendering an error rather than a success. Key absent: the form validates and then says "Message delivery is not connected yet… it has not been sent", pointing at the support email and the WhatsApp button. **The prompt's two halves conflicted** — a cosmetic "Message sent" toast *and* not pretending to send — and the honesty half won: a success message for a message that went nowhere is a lie the shopper acts on and never gets a reply for. The cosmetic toast reads "Message checked". Both branches are tested, including an assertion that the unconfigured path never calls `fetch` and that the words "Message sent" appear nowhere. **A copy bug found by reuse:** sharing `validateName`/`validateEmail` with the contact form exposed that their messages read "Enter the full name **for delivery**" and "Enter an email **for your order confirmation**" — wrong on a contact form. Both are now neutral ("Enter a name", "Enter an email address") since the field label already supplies the context; the address-page test asserting the old string was updated. **New components:** `Prose`, `PolicyPage`, `PolicyDisclaimer`, `TextAreaField`, `TextAreaFieldPreview`, `ContactDetails`, `ContactForm`. **New lib:** `lib/contact.ts` (validators reusing the address ones, plus the Web3Forms payload builder and the key accessors), `lib/metadata.ts` (`buildPageMetadata`, restating the full OG block once so ADR-007's replace-not-merge trap is encoded rather than remembered), `formatPolicyDate` in `lib/format.ts`, and `CONTACT_CONFIG` / `LEGAL_CONFIG` / `RETURN_WINDOW_DAYS` in `lib/config.ts` so every business detail is swappable in one file. **Footer** regrouped to `lg:grid-cols-6` with the brand spanning two and a new Policies column; all seven internal footer links were followed against the running build and return 200. **SEO:** About and the four policies indexable with 10 OG tags each and correct canonicals; `/contact` sets no `openGraph` at all so it inherits the layout's intact — that is what "lean" means here, not fewer tags. All six pages use the `%s · Morchadi Gems` template. Only `ContactForm` is a client island; the other five ship no page-specific JS. **Testing:** `lib/contact.test.ts` (19) and `lib/contact-form.test.tsx` (9) — suite now **229 passing across 9 files**. **Build incident worth recording:** the first `next build` failed with `PageNotFoundError: Cannot find module for page: /_document`. Not a code fault — a `next dev` server had started concurrently and was writing to the same `.next` directory, alongside two `next start` servers left running by prompts 9 and 10 (an earlier `pkill -f "next start"` had matched its own invoking shell and killed that instead of the servers). Stopping all three and rebuilding produced a clean build; nothing in the page code changed. **Not visually verified:** no browser in this environment — the six pages were checked by asserting on served HTML, so the prose rhythm, the footer at six columns and the contact page's two-column split have not been looked at. Still 404 pending: `/payment`. | `docs/decisions/ADR-012-static-and-policy-pages.md`, `docs/decisions/README.md`, `docs/design/DESIGN_SYSTEM.md`, `docs/progress/BUILD_LOG.md` | Complete |
| 12 | 2026-08-17 | **Cashfree order creation + the payment page — the money path.** **Pricing core:** `lib/order.ts` — `buildOrderFromCart(items, catalogue)` is the one place an order total is produced, and it is a pure function whose signature has nowhere to put a price. `items` is `{productId, qty}[]`; a tampered amount is not rejected but *unrepresentable*. Three properties are load-bearing and each has a test: a refused order returns zeroed amounts and no line items (so a caller that ignores `valid` still cannot charge anything); every fault is collected rather than short-circuited; and the `catalogue` parameter is typed as `OrderPricingEntry` — `id`/`name`/`price`/`inStock`, **no `mrp` field** — so ADR-003's display-only rule became a compile-time fact instead of a convention. `parseOrderItems` reduces an arbitrary JSON body to that shape and drops any `price`/`total`/`name` the client attached. **Duplicate product ids are refused, not merged** — merging would let a hand-made request send one id five times at qty 10 and buy fifty of a piece capped at ten. **Route:** `app/api/create-order/route.ts`, `runtime = "nodejs"`, `force-dynamic`, `Cache-Control: no-store` on every response. It re-validates the address with the same `validateAddressForm` the client used, because "the client said it was valid" is not a fact the server has; items are checked before the address so a shopper with two problems is sent to `/cart` first. `order_id` is `MG_{epoch ms}_{8 base36}` from `node:crypto` (25 chars, inside Cashfree's 50-char limit, sorts chronologically); `customer_id` is a fresh `guest_{12 base36}` that links to nothing. Direct `fetch` to `{base}/pg/orders` with `x-api-version: 2025-01-01` and a 15s timeout — no Cashfree Node SDK. The 200 body echoes `mode` because the browser SDK must be initialised against the environment its session was minted in, and the client cannot read `CASHFREE_ENV`. **`CASHFREE_ENV` resolves to production only on the exact string `production`**, so a typo fails towards sandbox. Upstream status and body are logged in full server-side and never returned; a missing credential is a distinct 503 `PAYMENT_NOT_CONFIGURED` rather than a 502, because retrying cannot fix it. **Secrets:** `lib/cashfree-config.ts` opens with `import "server-only"`, which is why it exists separately from `lib/config.ts` (client components import that one). **Verified by deliberately breaking it** — importing it into `PaymentCheckout.tsx` failed the build with the server-only error; reverted. Grepped `.next/static` afterwards: no `CASHFREE_`, no `X-Client-Secret`, no Cashfree API host in any client chunk. The only Cashfree string on the client is the public loader URL, in the separate chunk the dynamic import creates. **Payment page:** `/payment` (static, 2.57 kB) + the `PaymentCheckout` island. Four guards in order — empty cart, sold-out line, no address bundle, then pay — all decided **after** hydration, so the served HTML is the loading line with no amount and no button (asserted against the production build). The **double-submit guard is a `useRef` latch**, not the `disabled` attribute: disabling is a state update that lands a render later, so a fast double click would otherwise create two Cashfree orders; the ref is checked and set in the same synchronous tick, released on every failure path and deliberately not on success. `CheckoutGuardNotice` grew an optional `action` so the missing-address guard can point at `/address` instead of `/cart`. **Testing:** `lib/order.test.ts` — **31 new tests**, suite now **260 across 10 files**. The route's error paths were exercised against a real `next start` build, including a call to the Cashfree sandbox with deliberately wrong credentials: it authenticated, got a 401, and mapped to a clean 502 with the upstream body in the log only. **Deliberate 404 checkpoint:** `return_url` points at `/order-confirmation`, which does not exist yet — a completed sandbox payment lands on a 404 until the next prompt builds verification and confirmation. **Blocked on you:** no Cashfree credentials exist in this environment, so no real payment session has ever been created; the one line of the route that reads `payment_session_id` out of a success body, and `cashfree.checkout()` itself, are the only parts not yet executed. **Not visually verified:** no browser here — the payment page was checked by asserting on served HTML, so its two-column layout, the address recap and the error panel have not been looked at. | `docs/decisions/ADR-013-order-creation-and-payment.md`, `docs/decisions/README.md`, `docs/api/create-order.md`, `docs/api/README.md`, `docs/testing/PLAN-order-pricing.md`, `docs/testing/RESULT-2026-08-17-order-pricing.md`, `docs/testing/README.md`, `docs/progress/BUILD_LOG.md`, `.env.example` | Complete |
| B-fix | 2026-08-18 | Post-interruption fixes to Prompt B. Relaxed the over-strict `getByText("₹2,099")` assertion in the PAID confirmation test to `getAllByText(...)` — the amount correctly renders twice (once as "Amount paid", once as the receipt "Total") and the page was right, the assertion was not. Corrected the delivery estimate to the canonical site-wide wording "Dispatch within 2 business days · Delivery within 7 business days" by fixing `LEGAL_CONFIG.dispatchWindow` / `deliveryWindow`, which `DELIVERY_ESTIMATE_LINE`, `/shipping` and `/refund` all read, so the confirmation promise and the policy pages stay in step. Gate green: typecheck, lint, 328 tests, product validation, build. | `lib/config.ts`, `lib/order-confirmation.test.tsx` | Complete |
| 14 | 2026-08-18 | **The real business identity, and free shipping over ₹799.** **`config/business.ts` is now the owner's file** — a new top-level `config/` holding one `BUSINESS` object of eight plain literal fields (legal entity, brand, jurisdiction city/state, support email, phone, WhatsApp digits, address lines), with no logic, no derived values and no imports. `lib/config.ts` imports it and stays what it always was — the *site's* config, mapping those facts into `SITE_CONFIG` / `CONTACT_CONFIG` / `LEGAL_CONFIG`, deriving what can be derived, and owning the service promises that belong to the site rather than to the entity (hours, reply window, dispatch/delivery windows, payment provider). Nothing outside `lib/config.ts` imports `config/business.ts`, so **no call site changed** — pages and components read `lib/config.ts` exactly as before. **Every placeholder is gone:** `910000000000` → `919358358834`, `hello@morchadigems.example` / `privacy@morchadigems.example` → `admin@morchadigems.com`, `[REGISTERED ENTITY NAME]` → Morchadi Enterprise, `[CITY]`/`[STATE]` → Jaipur/Rajasthan, the five-line bracketed address → the real three-line one, and the four `[PLACEHOLDER — confirm]` markers on `/refund` resolved into actual policy. **Two keys deleted, not repointed:** `privacyEmail` and `orderSupportEmail` are gone — three fields that must always hold the same address is drift waiting to happen, so `/privacy` and the confirmation page now read `supportEmail` like everything else. That also closed the `.example`-vs-real-domain discrepancy flagged in the prompt-13 row. `phoneHref` is *derived* from `phoneDisplay` by stripping non-digits rather than being a second field; `whatsappNumber` stays separate because a chat number legitimately differs from a landline. The full address now renders on `/contact` and in the footer (brand column), and the contact page's WhatsApp entry became a real `wa.me` link instead of "use the button in the corner". **Shipping: free at or above ₹799, ₹99 below, boundary inclusive.** `FREE_SHIPPING_THRESHOLD = 799` joined `FLAT_SHIPPING_RATE = 99`, and — the load-bearing part — **the comparison is shared, not just the constants**: `calculateShipping(subtotal)` in `lib/config.ts` is called by both `calculateCartTotals` (`lib/cart.ts`, what the shopper sees) and `buildOrderFromCart` (`lib/order.ts`, what is charged). Sharing only the constants would have left the `>=`-vs-`>` decision to be made twice, and the failure mode there is the worst available: the cart says free, the server charges ₹99, and the card is debited an amount the page never showed. Its `subtotal <= 0` guard replaced the cart's old "charge shipping only when a payable line exists" check — the same property expressed on the subtotal instead of the line list, which is precisely why one function can serve both callers. **Trust boundary unchanged:** shipping is still derived server-side from a catalogue-priced subtotal; `buildOrderFromCart`'s signature still has nowhere to put a client amount, `mrp` is still absent from the pricing types, and a client claiming to have earned free delivery is not sending an input to anything. **Display, all read from the constants, none written as literals:** TrustStrip "Free Shipping Over ₹799", AnnouncementBar rebuilt from `FREE_SHIPPING_THRESHOLD`/`RETURN_WINDOW_DAYS` instead of three hardcoded strings, and `OrderTotals` with a three-state shipping row — the amount, `FREE`, or `—`. `FREE` and `—` are kept distinct deliberately: an empty cart has not *earned* free shipping, it has nothing to ship, and `FREE` there reads as a promise. A `gold-deep` "Add ₹X for free shipping" hint appears while `0 < subtotal < 799`, computed by `amountToFreeShipping` — display only, never part of a total. `/shipping` rewritten (inclusive boundary stated in words, plus a line saying the subtotal is decided on our server), `/terms`, `/about` and the `/shop` metadata brought in line, still India-only throughout. **A copy consequence worth noting:** prose that named the rate no longer works under a conditional rule — `/refund`'s "the ₹99 shipping charge" became "any shipping charge paid on the original order", because whether one was paid now depends on the order. **Testing:** new `PLAN-shipping-threshold.md` + result, 20/20. Nine new cases (798/799/800 in *both* pricing modules, a sold-out line that cannot buy its way past the threshold, shipping-once across several below-threshold lines) and eleven existing assertions rewritten where they assumed a flat rate — the shared fixtures price at ₹1,000, which now ships free, so `₹2,099` expectations became `₹2,000` plus an explicit `FREE`. `MAX_QUANTITY` is 10, so the boundary amounts are unreachable via quantity; both suites gained a helper that prices a single piece at the subtotal under test. Suite now **337 passing across 12 files** (was 328). **Judgement calls the owner should confirm:** the four resolved `/refund` placeholders — made-to-order, pierced jewellery on hygiene grounds and clearance items are non-returnable, and no pickup is arranged for change-of-mind returns (collection stays at our cost for faulty or incorrect pieces). These are business-policy calls; they are written into the policy rather than left as bracketed text a shopper can see, and are flagged in ADR-015 for review. **ADR numbering:** this is ADR-015 as the prompt specified; there is no ADR-014 — prompt 13 shipped without one, so the index skips it rather than renumbering an accepted record. **Not visually verified:** no browser here — the footer's new address block, the `FREE` row and the shortfall hint were checked by asserting on rendered HTML and on the built output, not looked at. | `docs/decisions/ADR-015-business-config-and-shipping-threshold.md`, `docs/decisions/README.md`, `docs/design/DESIGN_SYSTEM.md`, `docs/testing/PLAN-shipping-threshold.md`, `docs/testing/RESULT-2026-08-18-shipping-threshold.md`, `docs/testing/README.md`, `docs/progress/BUILD_LOG.md` | Complete |
| 15 | 2026-08-18 | **The owner's 21 real products replace the invented rings, and options land as data.** **`id` is the P-code.** P001–P021 keep exactly the numbering the owner uses on invoices, photo filenames and every message about stock, and the ADR-006 image derivation is untouched — `/products/P001.webp`, 21 real photographs now sitting on disk beside 79 generated placeholders. The `{prefix}-NNN` convention survives for the placeholders and `validate:products` accepts either form, applying the category-prefix rule only to non-P-codes. Two schemes coexist on purpose: it is how you tell an invented row from a real one at a glance. **Options are recorded a prompt before they are wired.** `Product` gains `options?: ProductOption[]` (`{ name, values }`); P001 and P005 carry a `Letter` (25 and 22 values), P006 a `Shape`, P010 a `Colour`. Nothing reads the field — no selector, `CatalogueEntry` unchanged, cart lines still keyed by product id alone — because changing what a cart line *is* touches the money path and deserves its own change. The validator checks option shape today so a malformed option cannot sit unnoticed while nothing renders it. **Fixed attributes are `details`, not options:** `ProductDetails` gains optional `stone` and `size`, and `ProductDetailsList` renders them — a green stone is a property of the ring, a plating colour is a choice, and that line is what keeps the option model honest. **Checks written for invented data now apply only to invented data:** id convention, the 60% discount ceiling (80% for the owner's rows — P020 is ₹130 against ₹599) and a required `details.weight` are scoped to placeholders; a weight is a factual claim to a buyer and none was supplied, so those rows have no Weight line rather than a made-up one. Everything else — uniqueness, image path and file existence, `mrp >= price`, ratings, reviews, flags, stray keys — is checked identically on all 100. **The 18 real rings replace all 13 `rg-*` rows**, and eight further placeholders (the most duplicative row in each of the other categories) were trimmed to hold the catalogue at exactly 100: `nk-011 nk-012 er-013 br-012 bn-013 pd-011 ak-011 np-009`. The three placeholder sold-outs were kept and the validator now asserts *placeholder* sold-outs specifically, so coverage of the sold-out UI at other price points cannot be trimmed away later. P004 and P012 stay as two listings — the owner's merchandising call on an identical ring. Home now surfaces 10 of the owner's products across Best Sellers and New Arrivals, with the remaining 6 flags on placeholders so all eight categories stay represented. Recorded as a launch blocker: the 21 real products carry invented ratings and reviews drawn from the placeholder pool, because the shop has taken no orders yet. Gate green — typecheck, lint, 337 tests across 12 files (unchanged; no test hardcoded a count or an id), `validate:products`, and a build of 100 product pages. | `docs/decisions/ADR-016-real-product-import.md`, `docs/testing/PLAN-product-catalogue.md`, `docs/testing/RESULT-2026-08-18-product-catalogue-real-import.md`, `docs/design/IMAGES.md`, `docs/progress/BUILD_LOG.md` | Complete |
| 16 | 2026-08-18 | **The final content pass: the four policies and `/about` rewritten to describe the shop that actually exists.** **The copy described the wrong business.** ADR-012 generated the policies from what the *code* does, and it did that well — but the prose described a hallmarked fine-jewellery workshop, while the ADR-016 catalogue is handcrafted and curated **artificial** jewellery priced from ₹130. `/terms` §3 now says so plainly — not precious metal, not precious stone, not sold as an investment — and every hallmark claim is gone from all five pages. **Everything the copy states is an expression, not a literal.** `config/business.ts` gains the story facts the owner will change (`foundedYear` 2016, `customersServed` 10,000, `designsReleased` 500, `deliveryCoverage` Pan India) surfaced as `STORY_CONFIG`; `LEGAL_CONFIG` gains `damageReportWindow` (48 hours), `replacementDispatchWindow` (7 working days), `minimumAge` (18) and `shippingScope` (India), and `refundProcessingWindow` moves 5–7 → **7–10 business days** as one constant edit. `formatMilestone` (`10000` → `"10,000+"`) went into `lib/format.ts` beside `formatRupees`, not into config — config holds facts, formatting them is a different job. Audit of the built HTML: only `2`, `7` and `7–10` appear as day-numbers across all five pages, and only `₹99` and `₹799` as amounts. **`/terms`** gained Introduction, Eligibility (18+ or guardian), Products, Orders (subject to acceptance and availability), Prohibited Use and a Changes clause, and **lost its user-accounts clause** — guest checkout is a privacy fact, not a term of sale, so it lives in `/privacy` only. **`/privacy`** gained usage data, an explicit Payments section (Cashfree hosted, card data never reaches us), Cookies merged with the `localStorage`/`sessionStorage` account, How-we-use, Sharing (no sale or rent; processor, delivery partners, legal authorities) and Data Security. **`/refund`** states the four owner-approved carve-outs as rules rather than the bracketed questions ADR-015 left: made-to-order and personalized pieces (letter rings) non-returnable **unless damaged or defective**, pierced jewellery non-returnable on hygiene grounds, clearance final, and return shipping split by cause — change-of-mind at the customer's cost with no pickup, faulty or incorrect collected at ours. **`/shipping`** gained a 48-hour damage-report window with photographs, a replacement dispatched within 7 working days, delivery conditions (not liable for courier delays or post-delivery loss), a tracking-may-take-24-hours note, and a policy-updates clause. **`/about` rebuilt from existing primitives — no new component, no new icon.** Est. eyebrow, a two-tone `Crafted With Love. / Worn With Pride.` hero with a `/shop` CTA, Our Story, a stat band as a `<dl>` (label-value pairs, so a screen reader reads "Founded, 2016"), a five-stop journey as an `<ol>`, six `TrustBadge`s for Why Choose Morchadi, `TestimonialBand` as "Customer Love", and a closing CTA. `TrustStrip` stays the fixed four-badge home strip — `/about` maps its own six over `TrustBadge` rather than growing a variant prop. `TestimonialBand` gained optional `roman`/`accent`/`subtitle` props defaulting to its current words, so the home page is byte-identical; the data source stays inside the band, not a prop. Full OG restate kept, so `/about` stays indexable. **Gate green:** typecheck, lint, **337 passing across 12 files (unchanged — no test asserts policy or about copy)**, `validate:products`, and a build of 117 static pages. Grep-verified: no "Morchadi Jewels", no `morchadijewels.com`, no international-shipping language, no accounts language outside `/privacy`'s statements that there are none. **Launch blocker surfaced and deliberately not fixed:** `/terms` now says artificial jewellery, and seven surfaces outside the five pages in scope still claim "hallmarked" — `AnnouncementBar`, `Hero`, `Footer`, `CartEmptyState`, `app/page.tsx`, `app/shop/page.tsx` and `SITE_CONFIG.description`. Global chrome puts two of them on the very pages carrying the new clause. BIS hallmarking applies to gold and silver; claiming it for artificial jewellery is a false description. Rewriting the storefront's brand voice is a marketing call for the owner, so it is listed in ADR-017 rather than made here. **Not visually verified:** no browser — the new `/about` sections were checked by asserting on the built HTML, not looked at. | `docs/decisions/ADR-017-final-content-pass.md`, `docs/decisions/README.md`, `docs/design/DESIGN_SYSTEM.md`, `docs/progress/BUILD_LOG.md` | Complete |
| 17 | 2026-08-18 | **The "hallmarked" claim is gone from the storefront, and the ADR-017 launch blocker is closed.** Hallmarking is not an adjective — under the BIS regulations it is a statutory certification scheme for articles of gold and silver, carried as a physical mark and a HUID. It has no meaning for plated brass, alloy or stainless steel, so on this catalogue it was a false description of goods, not enthusiastic copy. Two of the offending surfaces were global chrome, which meant a shopper could read "Certified & hallmarked jewellery" in the strip at the top of `/terms` and "not precious metal or precious stone jewellery" in the body below it. **The replacement vocabulary was already the brand's own:** the `/about` timeline records 2020 as the year the collection moved onto an **anti-tarnish** plating — a real, testable property customers actually ask about — so `anti-tarnish`, `hand-finished`, `skin-friendly` and `quality-checked` do the work that `hallmarked` and `certified` were doing. **Nine surfaces changed, not the seven ADR-017 listed.** `SITE_CONFIG.title` "Fine Jewellery Online" → "Artificial Jewellery Online" and `ogImage.alt` "fine jewellery" → "artificial jewellery" were not on the blocker list and needed to be: **"fine jewellery" is a term of art too**, denoting precious metal and genuine stones as against fashion jewellery, so replacing "hallmarked" while leaving the site titled "Fine Jewellery Online" would have fixed the word and kept the claim. It is also the phrase Indian shoppers actually search. Plus `SITE_CONFIG.description`, `AnnouncementBar` ("Certified & hallmarked jewellery" → "Anti-tarnish, skin-friendly jewellery"), `Hero` eyebrow and lede (the lede's "temple gold and oxidised silver" → "Gold-plated, anti-tarnish and kind to skin"), `Footer`, `CartEmptyState` ("everyday oxidised silver to kundan" → "everyday studs to statement pieces"), `TrustStrip` badge four (**"Certified Quality / Every piece inspected" → "Anti-Tarnish Quality / Checked by hand before dispatch"** — the last certification claim in the chrome, now a description of what we do rather than of a document we do not hold), `app/page.tsx`, `app/shop/page.tsx` (×2 + metadata) and `data/testimonials.json`. **Fixed at the config source:** `PRODUCT_DESCRIPTOR` (`"anti-tarnish, skin-friendly artificial jewellery"`) is exported from `lib/config.ts` and read by both `SITE_CONFIG.description` and the per-category description `app/shop/page.tsx` builds — those two near-identical sentences were independently written down, which is exactly how "hallmarked" survived in two places at once. The prose surfaces keep their own words; forcing one phrase through Hero, Footer and CartEmptyState would read like a filled-in template. **The same defect was one layer down in the catalogue.** The no-new-false-claims sweep found nine placeholder products asserting materials they are not: `np-002` "18k gold with a certified solitaire diamond" → **Crystal Solitaire Nose Pin**, "Gold-plated brass with a cubic zirconia solitaire"; `np-008` **Emerald Green Stud Nose Pin**; and seven `925 sterling silver` rows moved to `Silver-plated brass` or `Oxidised German silver` (`bn-011`, `pd-008` → **Evil Eye Nazar Pendant**, `ak-001` → **Ghungroo Payal Pair**, `ak-006`, `ak-008`, `np-005`, `np-012` → **Clip On Nose Ring**). Five names changed because the name carried the claim — a piece called *Diamond Solitaire* is a diamond claim however carefully the material line is worded. All nine are invented placeholder rows, none is an owner P-code, and no test referenced them. **Two things deliberately left alone:** *German silver* stays on the six oxidised rows — it is a copper-nickel-zinc alloy containing no silver and the standard honest trade term for this category, and replacing an accurate term for containing the word "silver" would trade precision for squeamishness; and `18K gold plated` stays on all fourteen of the owner's real products, because plating karatage states the karat of the **layer** and "plated" is doing the work. **The reworded testimonial deserves a note:** `data/testimonials.json` is invented placeholder copy, so editing it is editing our own fiction. Had it been a real quotation it would have been deleted rather than reworded — you do not edit a customer's words to fit a policy. **Gate green:** typecheck, lint, **337 passing across 12 files (unchanged — no test asserted the old copy or referenced the nine renamed products)**, `validate:products`, and a build of 117 static pages. **Grep proof: zero `hallmark` strings in code, data or `.next` rendered output**; the only remaining mentions are this row, ADR-017, ADR-018 and the immutable `RESULT-2026-08-17-shop-logic.md` snapshot, all of them historical records. Re-audited the whole catalogue for `925`, `sterling`, `22K`, `916`, `BIS`, `solid gold`, `real gold/silver` and `certified` — **zero**. **Not visually verified:** no browser — the chrome and hero changes were checked by asserting on the built HTML, not looked at. | `docs/decisions/ADR-018-honest-product-description.md`, `docs/decisions/README.md`, `docs/design/DESIGN_SYSTEM.md`, `docs/progress/BUILD_LOG.md` | Complete |
| 18 | 2026-08-18 | **Product options (variants) — a recorded choice, never a price.** The four owner products that are made to the buyer's choice (P001 and P005 Letter, P006 Shape, P010 Colour) now sell that choice end to end, and the money path is provably untouched by it. **The cart's unit of work moved from the product id to a line key.** `lineKey(productId, selectedOptions)` in the new `lib/options.ts` is the product id alone when there is no selection, and otherwise the id plus the selection's `name=value` pairs **sorted** (so record order cannot make two keys for one choice) and **percent-encoded** (so a value containing `\|` or `=` cannot forge another line's key). Add, increment, set-quantity, remove, dedupe and stale-prune all key on it. Because the key *is* the product id for the ninety-six products without options, **every one of the 337 pre-existing tests passed unmodified** — not one was edited to accommodate the feature. **Defaults with visibility:** every group is pre-selected with its first value, so a personalized piece is addable from a product page or a shop card without opening a selector (`AddToCartButton` needed no change — `addProductToCart` resolves whatever it is given, including nothing, against the entry's current options). The selection is echoed above the buy buttons, on every cart line, in the checkout summary and on the confirmation receipt, because a default nobody picked still has to be one they can see. **Selectors** (`ProductOptionSelector`): six values or fewer render as radio chips, more become a native select — 25 engraving letters as chips are a wall, four locket shapes in a dropdown hide the choice. Chips are visually-hidden `<input type="radio">` with styled labels, so arrow-key navigation, the checked state and the group's accessible name come from the platform rather than being re-implemented on buttons. **Stale-prune got stricter in one direction only:** a persisted line whose selection names a group or value the catalogue no longer offers is **dropped**, because the line describes a piece we cannot make and substituting the nearest letter would ship somebody an engraving they did not ask for; a line that is merely incomplete (the product gained a group after it was added) is **kept and defaulted**. **The order side needed one careful move.** `buildOrderFromCart` deliberately refuses a repeated product id, and options give one product several lines — so `mergeOrderItemsByProduct` sums a product's lines *before* pricing rather than merging inside the pricing core, which keeps the anti-abuse guard intact: the **summed** quantity is what gets bounds-checked, so five `A` rings and six `B` rings is still refused with `INVALID_QUANTITY`. `buildOrderFromCart` itself is byte-for-byte unchanged and still cannot see an option; `lib/order-options.ts`, the only server module that handles selections, is typed against an entry with **no `price` field at all**. It validates each line against `data/products.json` — a withdrawn value is refused with a new `INVALID_OPTION` code rather than silently resolved to a default — and builds a compact `P001:Letter=A; P010:Colour=Golden` summary that travels to Cashfree as `order_tags`, split across up to three values for the 255-character limit with a `+N more` marker if even that overflows. With no database the payment record is the order record, and that is where the next prompt's fulfilment message will read what to engrave. **An order of the ninety-six plain products sends exactly the request body it sent before this prompt.** **Personalized · non-returnable** surfaces with the option: in full next to the buy actions with a link to `/refund`, and as the label alone on the cart line. **The trade-off worth knowing:** a product's per-product quantity cap of 10 is now shared across all of its selections, because lines merge before the cap is checked. That is the correct reading of "at most ten of a piece", but it is reachable from `/cart`, where each line can independently be set to 10. **Gate green:** typecheck, lint, **433 passing across 15 files** (up from 337 across 12 — 96 new, none edited), `validate:products` (`PASS`, no change needed — it already validated `options`), and a clean build of 117 static pages. **Verified in the built HTML**, not just in tests: P001 and P005 render a select, P006 and P010 render radios, all four carry the note, and P002 renders none of it. **Not visually verified:** no browser — the selectors were checked by rendering them in tests and by reading the built HTML, not looked at. **Not covered:** `order_tags` has never been sent to a live Cashfree order; the packing is unit-tested against the documented limit, but a sandbox payment should be run before production since a rejected field would break checkout for these four products only. | `docs/decisions/ADR-019-product-options.md`, `docs/decisions/README.md`, `docs/api/create-order.md`, `docs/design/DESIGN_SYSTEM.md`, `docs/testing/PLAN-cart-line-keys.md`, `docs/testing/RESULT-2026-08-18-cart-line-keys.md`, `docs/testing/README.md`, `docs/progress/BUILD_LOG.md` | Complete |
