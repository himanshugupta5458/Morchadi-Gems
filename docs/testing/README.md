# Testing

## Purpose

Test plans and the results of running them. A plan states what must be true before a
feature is considered shippable; a result records what actually happened when it was run,
including failures.

Money paths are the priority here. Server-side price validation, cart totals, and the
Cashfree order lifecycle each need a plan that includes hostile cases — a tampered price in
the request body, a quantity of zero or negative, an unknown product ID, a duplicate
webhook — not just the path where everything works.

## Naming convention

| Kind | Pattern | Example |
| --- | --- | --- |
| Plan | `PLAN-short-kebab-case-area.md` | `PLAN-checkout-flow.md` |
| Result | `RESULT-YYYY-MM-DD-short-kebab-case-area.md` | `RESULT-2026-08-17-checkout-flow.md` |

A result file links back to the plan it executed. A plan is a living document and may be
edited; a result is a snapshot and is never edited after it is written.

## Required structure — plan

```markdown
# Test Plan: Area

- **Scope:** what is covered, and explicitly what is not
- **Prerequisites:** env vars, sandbox credentials, seed data

## Cases

| ID | Scenario | Steps | Expected result | Type |
| --- | --- | --- | --- | --- |
| TC-01 | ... | ... | ... | Automated / Manual |

Include negative and adversarial cases, not only the happy path.
```

## Required structure — result

```markdown
# Test Result: Area — YYYY-MM-DD

- **Plan:** [PLAN-area.md](PLAN-area.md)
- **Commit:** short SHA
- **Environment:** local / container build, Cashfree sandbox or production

| ID | Result | Notes |
| --- | --- | --- |
| TC-01 | Pass / Fail | ... |

## Failures
One subsection per failure, linking to the log in `../logs/` if one was written.

## Summary
N passed, N failed, N skipped — and whether this is shippable.
```

Record failures as failures. A result file that hides a failing case defeats its own
purpose.

## Index

| Plan | Latest result |
| --- | --- |
| [PLAN-product-catalogue.md](PLAN-product-catalogue.md) | [2026-08-17](RESULT-2026-08-17-product-catalogue.md) — 22/22 pass |
| [PLAN-shop-logic.md](PLAN-shop-logic.md) | [2026-08-17](RESULT-2026-08-17-shop-logic.md) — 53/53 pass |
| [PLAN-cart-logic.md](PLAN-cart-logic.md) | [2026-08-17](RESULT-2026-08-17-cart-logic.md) — 59/59 pass |
| [PLAN-address-validation.md](PLAN-address-validation.md) | [2026-08-17](RESULT-2026-08-17-address-validation.md) — 83/83 pass |
| [PLAN-order-pricing.md](PLAN-order-pricing.md) | [2026-08-17](RESULT-2026-08-17-order-pricing.md) — 36/36 pass |
| [PLAN-shipping-threshold.md](PLAN-shipping-threshold.md) | [2026-08-18](RESULT-2026-08-18-shipping-threshold.md) — 20/20 pass |
| [PLAN-cart-line-keys.md](PLAN-cart-line-keys.md) | [2026-08-18](RESULT-2026-08-18-cart-line-keys.md) — 85/85 pass |
| [PLAN-catalogue-ia.md](PLAN-catalogue-ia.md) | [2026-08-18](RESULT-2026-08-18-catalogue-ia.md) — two-tier catalogue IA |
| *(no plan — data import verification)* | [2026-08-18](RESULT-2026-08-18-product-catalogue-real-import.md) — the owner's real catalogue imported under P-code ids |
| *(no plan — data correction)* | [2026-08-18](RESULT-2026-08-18-all-real-catalogue.md) — the last invented products removed, all 49 real |
| *(no plan — regression guards)* | [2026-08-18](RESULT-2026-08-18-funnel-ui-polish.md) — funnel UI polish, 480/480 pass |
| *(no plan — regression guard)* | [2026-08-18](RESULT-2026-08-18-button-padding.md) — button padding verified against the emitted CSS, 481/481 pass |
| *(no plan — regression guard)* | [2026-08-18](RESULT-2026-08-18-hero-cta-equal-width.md) — hero paired CTAs equal width, 485/485 pass |
| [PLAN-product-schema-migration.md](PLAN-product-schema-migration.md) | [2026-08-18](RESULT-2026-08-18-product-schema-migration.md) — 49/49 pass, 543/543 suite |
| [PLAN-seo-foundations.md](PLAN-seo-foundations.md) | [2026-08-18](RESULT-2026-08-18-seo-foundations.md) — 58/58 pass, 601/601 suite |
| *(no plan — regression guard)* | [2026-08-19](RESULT-2026-08-19-mobile-scale.md) — the mobile type and spacing scale, desktop verified unchanged, 676/676 suite |
| *(no plan — deployment verification)* | [2026-08-19](RESULT-2026-08-19-container-build.md) — containerised production build, 43/43 as expected, 653/653 suite |
| *(no plan — regression guard)* | [2026-08-19](RESULT-2026-08-19-mobile-layout.md) — four mobile layouts differing in kind, plus a pre-existing overflow bug, 690/690 suite |
| [PLAN-seo-audit-remediation.md](PLAN-seo-audit-remediation.md) | [2026-08-19](RESULT-2026-08-19-seo-audit-remediation.md) — 70/70 pass, 735/735 suite |
| *(no plan — live-site audit)* | [2026-08-19](RESULT-2026-08-19-seo-audit-followup.md) — post-remediation SEO audit, 66/100, 3 criticals live, no code changed |
| *(no plan — content and data correction)* | [2026-08-19](RESULT-2026-08-19-catalogue-content-pass.md) — catalogue content pass, 15/15 pass, 747/747 suite |
| *(no plan — metadata pass)* | [2026-08-19](RESULT-2026-08-19-product-seo-metadata.md) — per-product search and social metadata for all 49, 762/762 suite |
| *(no plan — live-site audit)* | [2026-08-19](RESULT-2026-08-19-seo-audit-round-three.md) — third-round SEO audit, no code changed |
| *(no plan — live-site audit)* | [2026-08-22](RESULT-2026-08-22-seo-audit-round-four.md) — fourth-round SEO audit, 75.7/100, no Critical confirmed, the round-three return-policy finding withdrawn on all-49 evidence, no code changed |
| *(no plan — schema verification)* | [2026-08-20](RESULT-2026-08-20-orders-crm-schema.md) — the orders/CRM migration applied and inspected in Postgres, `pricing.cost` proven absent from the built bundle, 814/814 suite |
| [PLAN-admin-auth.md](PLAN-admin-auth.md) | [2026-08-20](RESULT-2026-08-20-admin-auth.md) — admin login, sessions and subdomain routing, 73/73 pass, two defects found live and fixed, 895/895 suite |
| [PLAN-admin-auth.md](PLAN-admin-auth.md) — *Seed script prompts* | [2026-08-20](RESULT-2026-08-20-seed-admin-prompt.md) — the hidden password prompts made visible again, 20/20 pass, the regression test proven to fail on the old code, 907/907 suite |
| *(no plan — developer tooling)* | [2026-08-20](RESULT-2026-08-20-dev-all-command.md) — `npm run dev:all`, 38/38 pass plus five real runs including a cold start and a fresh volume, 945/945 suite |
| [PLAN-analytics-and-utm.md](PLAN-analytics-and-utm.md) | *(no separate result — verified by `lib/analytics.test.tsx`, `lib/utm.test.ts` and `lib/utm-ssr.test.ts`, and by the CSP assertions in `lib/security-headers.test.ts`)* |
| [PLAN-order-capture.md](PLAN-order-capture.md) | [2026-08-20](RESULT-2026-08-20-order-capture.md) — checkout wired to Postgres; the customer/order/line-item/history write, the per-choice line grouping, and the proof that a dead database changes no byte of the checkout response |
| [PLAN-admin-orders-list.md](PLAN-admin-orders-list.md) | [2026-08-20](RESULT-2026-08-20-admin-orders-list.md) — the order list: tabs, status filter, search, date range, sort and pagination, all of it in the URL |
| [PLAN-admin-order-detail.md](PLAN-admin-order-detail.md) | [2026-08-20](RESULT-2026-08-20-admin-order-detail.md) — the order detail screen and its three write endpoints: lifecycle transitions, refund settlement, address correction and the COD/RTO receipt toggles, each re-validated server-side |
| [PLAN-public-order-tracking.md](PLAN-public-order-tracking.md) | [2026-08-20](RESULT-2026-08-20-public-order-tracking.md) — `/track` proven to leak none of twenty operator-only values, case-insensitive lookup, the lookup limiter, and `/track` absent from the emitted sitemap and disallowed in the emitted `robots.txt`; 32/32 pass, three mutations confirmed to break them, 1202/1202 suite |
| *(no plan — one bug fix and two features)* | [logs/2026-08-20](../logs/2026-08-20-admin-shows-the-wrong-customer-name.md) — the frozen customer name diagnosed and fixed, alongside the `localStorage` saved address and the home page's track-order box; 31/31 new, 1233/1233 suite |
| [PLAN-database-failure-surfaces.md](PLAN-database-failure-surfaces.md) | [2026-08-21](RESULT-2026-08-21-database-failure-surfaces.md) — `/api/health` and the per-surface failure decisions of [ADR-048](../decisions/ADR-048-database-health-and-failure-surfaces.md); 29/29 cases pass, seven mutations confirmed to break them, 1261/1261 suite |
| [PLAN-stage-0-migration-batch.md](PLAN-stage-0-migration-batch.md) | [2026-08-23](RESULT-2026-08-23-stage-0-migration-batch.md) — Stage 0 of the Odoo migration ([ADR-054](../decisions/ADR-054-stage-0-migration-batch-preparation.md)); 64 automated cases and 5 manual, all pass, 69/69 in the file and 1636/1636 suite. **Against synthetic fixtures — the real Phase B export has not been delivered** |
| *(no plan — an already-made owner decision implemented)* | [2026-08-23](RESULT-2026-08-23-category-vocabulary.md) — `gift-hampers` added across all four enumerations and the vocabulary/surfacing split of [ADR-055](../decisions/ADR-055-category-vocabulary-and-surfacing.md); 20/20 pass, both new validator guards proved by breaking them, 1656/1656 suite |
| *(no plan — a read-only audit, nothing executed against a plan)* | [2026-08-23](RESULT-2026-08-23-pre-migration-readiness-audit.md) — pre-migration readiness audit of the whole Draft A pipeline ([ADR-051](../decisions/ADR-051-draft-a-content-pipeline.md)–[ADR-055](../decisions/ADR-055-category-vocabulary-and-surfacing.md)) before the 542-product Odoo export arrives. **Diagnose-only: nothing was fixed.** 3 BLOCKING, 9 IMPORTANT, 6 MINOR findings against a fully green gate — the register append corrupts `drafts-in-progress.md` silently on the first real run, the Stage 0 image suggestions have no path across the extraction step, and `subcategory` plus four `original*` fields are captured and then dropped |
| *(no plan — three findings from the audit above, implemented)* | [2026-08-23](RESULT-2026-08-23-image-confirmation-provenance-and-draft-similarity.md) — BLOCKING-2, BLOCKING-3 and IMPORTANT-1 fixed together ([ADR-056](../decisions/ADR-056-image-confirmation-provenance-and-draft-similarity.md)): image suggestions gain `confirmed` and carry their `verifiedDistinct` evidence across extraction; `subcategory` and a server-only `migrationProvenance` block reach the record; the similarity gate scores drafts against each other. **The client-bundle seal is proved by grepping a real 75-page build with negative controls, not by a unit assertion** — 0 hits in `.next/static`, in 64 prerendered `.html` and in 62 `.rsc` flight payloads, against 61/61 for a value that does reach the browser. 1695/1695 suite, 87 files |
| *(no plan — one bug fix and nine corrections from the audit)* | [2026-08-23](RESULT-2026-08-23-register-append-and-audit-corrections.md) — B-1 fixed: `appendRegisterRows` now anchors to the register table's last row and re-parses its own output before keeping the write. **Proved against a copy of the real `drafts-in-progress.md`, not just a fixture** — the old code left the table at 1 row and produced 542 non-rows; the new code produces a 543-row, 6-column table with zero parse problems. Running against the real file also exposed a second fault the audit missed: the double-run guard matched an id **in prose**, so the first real batch would have been refused by a sentence. Plus I-2…I-5, I-7…I-9 and M-1. 1716/1716 suite; the real register is byte-identical afterward |
| [PLAN-checkout-payment-paths.md](PLAN-checkout-payment-paths.md) | [2026-08-28](RESULT-2026-08-28-checkout-payment-paths.md) — 47/47 pass; [2026-08-28](RESULT-2026-08-28-catalogue-floor-fixture-decoupling.md) — re-run after P001's floor, 1841/1841; [2026-08-29](RESULT-2026-08-29-online-payment-discount.md) — the [ADR-063](../decisions/ADR-063-online-payment-discount.md) discount, TC-48–TC-59, 3 new route-level cases against a real database, 1920/1920 suite |
| [PLAN-customer-order-confirmation-email.md](PLAN-customer-order-confirmation-email.md) | [2026-08-29](RESULT-2026-08-29-customer-order-confirmation-email.md) — 30/31 pass, one substituted (no Resend key/no internet), 1904/1904 suite; [2026-08-29](RESULT-2026-08-29-customer-order-confirmation-email-visual-template.md) — the branded template rewrite, 6/7 automated pass and one skipped (no database), 1908/1908 suite |
| [PLAN-admin-order-detail.md](PLAN-admin-order-detail.md) | [2026-08-29](RESULT-2026-08-29-admin-money-panel-payment-confirmation.md) — the Money panel's "Prepaid" figure no longer reads as a received payment ahead of Cashfree's own `PAID` status, 4/4 new pass, 1924/1924 suite |
| [PLAN-admin-product-management.md](PLAN-admin-product-management.md) | [2026-08-29](RESULT-2026-08-29-admin-product-management.md) — the admin product editor ([ADR-064](../decisions/ADR-064-admin-product-management.md)): 59/59 pass, **2071/2071 suite with 0 skips**. **The first result in this folder to write `data/products.json` from a running server** — a real edit driven through the live route produced a one-line diff, was visible on the next read and on the storefront under `next dev`, and rebuilt `data/keyword-map.json`; a stale version token was genuinely refused **409 `CONCURRENT_CHANGE`** with the file byte-identical either side and the earlier writer's change intact; a karat claim and a fractional price were refused **422** in the gate's own words, `210.5` reaching the validator un-rounded. Working tree reverted and clean afterward. Also settles what the prior gate's 10 skips were — **ten missing-build-artefact guards, no database guard** — so the suite skips nothing once a build exists, the client-chunk margin grep included |
| [PLAN-card-variant-selection.md](PLAN-card-variant-selection.md) | [2026-08-31](RESULT-2026-08-31-card-variant-selection.md) — the three card purchase modes of [ADR-067](../decisions/ADR-067-card-variant-selection.md) and the alignment of a grid row holding all three; **10/12 pass, 2 fail**, 58/58 in the four card test files. The first result in this folder measured in a **real browser**: a forced mixed row at five widths (375/414/768/1024/1440) shares one card height, one chip-row top and one action top in every case, and the two-line "Choose Your Options" label wraps *inside* its 44px box with zero overflow — the pixel facts the jsdom suite is structurally blind to. Both failures are **pre-existing and outside ADR-067's scope**: the price block is not height-reserved, so a discounted card's "N% OFF" chip wraps and makes its block 52px against a plain card's 28px (the row keeps its baseline anyway, `mt-auto` absorbing the slack above the button), and the fixed WhatsApp button covers **29% of P408's call to action** at 1440 — the letter ring ADR-067 exists to route to its own page |

## Runners

Two, answering different questions. Both must be green; neither replaces the other.

| Command | Covers |
| --- | --- |
| `npm run validate:products` | The **data** — `data/products.json` shape, conventions, and the image files it points at |
| `npm run test:run` | The **logic** — Vitest unit tests over `lib/`. `npm test` for watch mode |

Vitest is configured in `vitest.config.mts` and picks up `lib/**/*.test.{ts,tsx}`.

`vitest.config.mts` aliases `server-only` to that package's own `empty.js`. Next.js does the same
thing through the `react-server` export condition; plain Node resolution does not, so without the
alias importing any server-only module from a test throws on import. It is what lets the two
Prisma suites and the two admin suites import [`lib/prisma.ts`](../../lib/prisma.ts) at all.

Several suites need a reachable Postgres — every file that imports `lib/prisma.ts`, which now
includes the order-capture, admin-order and `/track` suites as well as the original connection,
schema, session and auth ones — and every one of them **skips with a printed reason rather than
failing** when there is none, so a fresh clone and a CI runner still exit 0.
`lib/track-build-output.test.ts` skips the same way when there is no `.next` to read. Each cleans up after itself: the schema suite rolls its transaction back, and the two
admin suites delete the throwaway admin they created.

Most suites run in the default `node` environment. A `.test.tsx` file that needs a DOM opts in
per file with a `/** @vitest-environment jsdom */` docblock on its first line — the environment
is a property of the file, so it is declared in the file rather than pattern-matched in config.
`jsdom` and `@testing-library/react` are devDependencies and never reach a build.

| Suite | Covers | Plan |
| --- | --- | --- |
| `lib/shop.test.ts` | Filtering, sorting, pagination, URL vocabulary | [PLAN-shop-logic.md](PLAN-shop-logic.md) |
| `lib/quantity.test.ts` | `clampQuantity` bounds, flooring, and non-finite input | — small enough to read directly |
| `lib/cart.test.ts` | Cart arithmetic — add/merge/clamp, remove, set quantity, pruning, hostile persisted data, subtotal/shipping/total, and line identity once a product has options | [PLAN-cart-logic.md](PLAN-cart-logic.md), [PLAN-cart-line-keys.md](PLAN-cart-line-keys.md) |
| `lib/options.test.ts` | `lineKey`, option defaults and resolution, staleness, hostile stored selections, the order summary | [PLAN-cart-line-keys.md](PLAN-cart-line-keys.md) |
| `lib/order-options.test.ts` | Merging a product's lines before pricing, order-time option validation, Cashfree `order_tags` packing | [PLAN-cart-line-keys.md](PLAN-cart-line-keys.md) |
| `lib/product-options.test.tsx` | The product-page selectors, the cart line echo, the personalized note, per-line edits | [PLAN-cart-line-keys.md](PLAN-cart-line-keys.md) |
| `lib/cart-context.test.tsx` | `CartProvider` hydration, `localStorage` persistence, the header badge, and the `/cart` view | [PLAN-cart-logic.md](PLAN-cart-logic.md) |
| `lib/address.test.ts` | Per-field and aggregate address validation, the Indian states list, focus order | [PLAN-address-validation.md](PLAN-address-validation.md) |
| `lib/checkout.test.ts` | The `sessionStorage` checkout bundle — assembly from cart lines, the recorded choices it carries, and parsing hostile stored data | [PLAN-address-validation.md](PLAN-address-validation.md), [PLAN-cart-line-keys.md](PLAN-cart-line-keys.md) |
| `lib/address-checkout.test.tsx` | `/address` — the empty-cart guard, blur/submit validation, focus, the handoff to `/payment`, repopulation | [PLAN-address-validation.md](PLAN-address-validation.md) |
| `lib/order.test.ts` | Server-side order pricing — catalogue-only totals, `mrp` exclusion, hostile items, per-item error collection, untrusted-body parsing, the free-shipping boundary | [PLAN-order-pricing.md](PLAN-order-pricing.md), [PLAN-shipping-threshold.md](PLAN-shipping-threshold.md) |
| `lib/button-styles.test.ts` | The two button scales by their literal padding and type classes, that only the box differs, and that no height or line-height class constrains them | [2026-08-18](RESULT-2026-08-18-button-padding.md) |
| `lib/hero-cta.test.tsx` | The hero call-to-action pair — one grid parent of two equal columns, `fullWidth` on both, one box across the pair, and the gap between them | [2026-08-18](RESULT-2026-08-18-hero-cta-equal-width.md) |
| `lib/copy-dashes.test.ts` | The em-dash sweep — catalogue strings, and every non-test source file with comments stripped | [2026-08-18](RESULT-2026-08-18-funnel-ui-polish.md) |
| `lib/structured-data.test.ts` | The JSON-LD builders — Organization, the OnlineStore/LocalBusiness node, WebSite, Product, Offer, the real return and shipping policies, the derived price validity, CollectionPage and ItemList, BreadcrumbList, and absolute URLs | [PLAN-seo-foundations.md](PLAN-seo-foundations.md), [PLAN-seo-audit-remediation.md](PLAN-seo-audit-remediation.md) |
| `lib/sitemap.test.ts` | The sitemap — all 49 products, every surfaced category and no pending one, populated collections, the excluded checkout and API paths, priorities and dates | [PLAN-seo-foundations.md](PLAN-seo-foundations.md) |
| `lib/robots.test.ts` | `robots.txt` — the allow rule, the disallow list shared with the sitemap, the absolute sitemap link, and the admin subdomain's own deny-all file | [PLAN-seo-foundations.md](PLAN-seo-foundations.md), [PLAN-admin-auth.md](PLAN-admin-auth.md) |
| `lib/json-ld.test.tsx` | The rendered `application/ld+json` block — every graph parses back, and a hostile product name cannot close the script tag | [PLAN-seo-foundations.md](PLAN-seo-foundations.md) |
| `lib/no-fabricated-reviews.test.tsx` | That no rating or review survives anywhere — the catalogue file, the schema, the rendered `ld+json`, a product card, and every source file under `app/` and `components/` — plus the product page's `og:type` | [PLAN-seo-audit-remediation.md](PLAN-seo-audit-remediation.md) |
| `lib/product-copy.test.ts` | The approved descriptions — word range, paragraph storage, no copy-pass review metadata — plus the catalogue's material honesty (no karat, hallmark, 916 or sterling claim; no cubic zirconia called crystal; every "Silver" name qualified), now swept across the `seo` strings as well | [2026-08-19](RESULT-2026-08-19-catalogue-content-pass.md) |
| `lib/product-seo.test.ts` | The per-product `seo` block — every field measured in code points against the bound for its surface, no duplicate `metaTitle` or primary keyword, one alt per photograph, the honesty rules applied to the metadata, and what `generateMetadata` actually publishes including the Twitter mirror and the absolute title | [2026-08-19](RESULT-2026-08-19-product-seo-metadata.md) |
| `lib/security-headers.test.ts` | The six response headers and every CSP directive, including the five Cashfree origins the checkout needs and the development-only `unsafe-eval` | [PLAN-seo-audit-remediation.md](PLAN-seo-audit-remediation.md) |
| `lib/shop-indexing.test.ts` | Canonical URLs with the sort stripped, and `noindex, follow` on a facet that matches nothing | [PLAN-seo-audit-remediation.md](PLAN-seo-audit-remediation.md) |
| `lib/catalogue-ia.test.ts` | The two-tier IA — the category tier, the collection tier, and the two nav dropdowns built from them | [PLAN-catalogue-ia.md](PLAN-catalogue-ia.md) |
| `lib/product-schema.test.ts` | The migrated record shape — grouped fields, option groups, per-variant images, multi-image products, and `toCatalogueEntry` | [PLAN-product-schema-migration.md](PLAN-product-schema-migration.md) |
| `lib/product-gallery.test.tsx` | The thumbnail strip, `resolveVariantImage`, the per-variant swap, and the cart line's thumbnail | [PLAN-product-schema-migration.md](PLAN-product-schema-migration.md) |
| `lib/option-controls.test.tsx` | The four named option controls — dropdown, swatch, pills, chips — and the contract every one of them keeps | [PLAN-product-schema-migration.md](PLAN-product-schema-migration.md) |
| `lib/money-path.test.ts` | The money path end to end — what the pricing catalogue may carry, a tampered request, and that a recorded choice, a variant image or a second gallery image changes no amount | [PLAN-order-pricing.md](PLAN-order-pricing.md), [PLAN-product-schema-migration.md](PLAN-product-schema-migration.md) |
| `lib/verify.test.ts` | Payment verification — Cashfree status normalisation and its fail-closed default, `order_amount` reading, order-id shape, response parsing, the stale-bundle guard, and failure descriptions | [verify-order contract](../api/verify-order.md) |
| `lib/order-confirmation.test.tsx` | `/order-confirmation` in every state — unusable link, verification in flight, PAID, PENDING, FAILED, NOT_FOUND, and when our own verification cannot answer | [verify-order contract](../api/verify-order.md) |
| `lib/notify.test.ts` | The admin order message, the CallMeBot URL, credential reading, and dispatch | [notify-admin contract](../api/notify-admin.md) |
| `lib/notify-client.test.tsx` | The notified flag, notifying on a paid order, and that the notification never reaches the customer | [notify-admin contract](../api/notify-admin.md) |
| `lib/notify-boundary.test.ts` | That the CallMeBot key never crosses into the browser bundle | [notify-admin contract](../api/notify-admin.md) |
| `lib/contact.test.ts` | Contact-form validation — the reused checkout validators, subject and message bounds, the aggregate validator, and the Web3Forms payload | — |
| `lib/contact-form.test.tsx` | `/contact` — validation, and the two branches with and without a Web3Forms key | — |
| `lib/wordmark.test.tsx` | `Wordmark`, including the text variant used on dark grounds | — |
| `lib/responsive-scale.test.ts` | The mobile scale's class pairs, and that no desktop breakpoint value moved | [2026-08-19](RESULT-2026-08-19-mobile-scale.md) |
| `lib/mobile-layout.test.tsx` | The mobile product cap and the four layouts that differ in kind from desktop | [2026-08-19](RESULT-2026-08-19-mobile-layout.md) |
| `lib/prisma-connection.test.ts` | That the Prisma singleton opens a real connection to the local Postgres and answers `SELECT 1`, and that one client survives a module re-evaluation. **Skips rather than fails when no database is reachable** — a fresh clone and CI have no Docker Postgres, and a connectivity check must not become a gate they cannot pass | [ADR-040](../decisions/ADR-040-postgres-for-orders.md), [DEV-DATABASE.md](../DEV-DATABASE.md) |
| `lib/admin-routing.test.ts` | The admin panel's address — `ADMIN_HOSTNAME` resolution, `Host` and `X-Forwarded-Host` parsing, which hostnames count as a development machine, the public URL of every admin page on both, and `decideAdminRoute` across the admin subdomain, the storefront domain and localhost. Also drives `middleware.ts` itself with real `NextRequest`s and asserts the rewrite, redirect and pass-through headers it emits | [PLAN-admin-auth.md](PLAN-admin-auth.md), [ADR-041](../decisions/ADR-041-admin-subdomain-and-auth.md) |
| `lib/admin-session.test.ts` | Sessions — creation, the seven-day expiry, that the token is stored only as a SHA-256 digest, validation of unknown/empty/expired tokens, destruction of one session and of all of an admin's, the expiry sweep, and the cookie attributes including `Secure` following the environment. **Skips with no database**, like the Prisma suites | [PLAN-admin-auth.md](PLAN-admin-auth.md), [admin-login contract](../api/admin-login.md) |
| `lib/admin-auth.test.ts` | Login and logout end to end through the real route handlers — bcrypt hashing and salting, correct credentials, and the anti-enumeration contract: wrong password and unknown username answered with **byte-identical** bodies at the same status, above the same timing floor. **Skips with no database** | [PLAN-admin-auth.md](PLAN-admin-auth.md), [admin-login contract](../api/admin-login.md), [admin-logout contract](../api/admin-logout.md) |
| `lib/seed-admin-prompt.test.ts` | The terminal reader behind `npm run seed:admin` — that a question is written and **nothing afterwards erases it**, that a secret is never echoed in whole or in part, that a backspace is erased when visible and silent when hidden, that arrow keys are dropped rather than typed, that three consecutive prompts each appear, and that a pasted CRLF or type-ahead cannot answer the following prompt. Drives `scripts/tty-prompt.mjs` with ordinary streams, which is possible only because it takes them as arguments | [PLAN-admin-auth.md](PLAN-admin-auth.md), [2026-08-20](RESULT-2026-08-20-seed-admin-prompt.md), [log](../logs/2026-08-20-password-prompt-never-appears.md) |
| `lib/dev-stack-plan.test.ts` | The decision logic behind `npm run dev:all` — whether `DATABASE_URL` is the local Docker Postgres or a remote server no local command should touch, which compose service publishes its port, and what a container's state and health mean for the wait. **Only `healthy` ends the wait**; a merely running container, and a service with no healthcheck at all, are both refused rather than assumed ready. Three cases guard the boundary instead: that `dev-stack` appears in neither the `Dockerfile` nor `build` nor `start`. Drives `scripts/dev-stack-plan.mjs`, which is pure by design so the suite needs no Docker daemon and no remote database | [2026-08-20](RESULT-2026-08-20-dev-all-command.md), [DEV-DATABASE.md](../DEV-DATABASE.md) |
| `lib/prisma-schema.test.ts` | That the orders/CRM schema accepts writes matching its own shape — a customer, order, line item and status-history row through the generated client, the declared defaults, `Json` round-trips, and a `returned` order with an orthogonal refund. Each case runs inside an interactive transaction unwound by a thrown sentinel, so it leaves the database exactly as it found it. Skips with no database, like its sibling | [ADR-040](../decisions/ADR-040-postgres-for-orders.md), [2026-08-20](RESULT-2026-08-20-orders-crm-schema.md) |
| `lib/order-tracking-page.test.tsx` | `/track` against a committed order fixture — that the rendered HTML and the page's own data carry **none** of twenty operator-only values (the operator, two typed reasons, the phone, the address, the payment type, the Cashfree id, the campaign, the line item and five money figures including `total_cost`), and that a lookup finds the same order lowercased, mixed-case or space-padded. Skips when no database is reachable | [PLAN-public-order-tracking.md](PLAN-public-order-tracking.md) |
| `lib/tracking-lookup-limit.test.ts` | The lookup limiter — the eight-a-minute threshold, both edges of the sliding window to the millisecond, that a refused attempt is not recorded, the store's own bound, the process-level counter `/track` actually calls, and how the client key is read from `x-forwarded-for` | [PLAN-public-order-tracking.md](PLAN-public-order-tracking.md) |
| `lib/track-build-output.test.ts` | That `/track` is absent from the emitted `sitemap.xml` and disallowed in the emitted `robots.txt` — read out of `.next/server/app/` and the standalone copy after a build, with controls proving the same greps find the shop. Skips when there is no build | [PLAN-public-order-tracking.md](PLAN-public-order-tracking.md) |
| `lib/saved-address.test.tsx` | The `localStorage` saved address — what is written and what is deliberately not, a malformed record refused whole, an unrecognised state blanked, the first-time visitor's empty form, the pre-fill notice, that typing never writes back, that nothing is submitted on the shopper's behalf, clearing, and the in-progress checkout outranking the last order | [ADR-046](../decisions/ADR-046-saved-address-in-local-storage.md) |
| `lib/home-track-order.test.tsx` | The home page's track-order entry point — that it mounts the very component `/track` mounts, that the `GET` form builds `/track?order_id=…` from what is typed, and that it needs no endpoint of its own | [ADR-045](../decisions/ADR-045-public-order-tracking.md) |
| `lib/order-capture.test.ts` | The capture write — cart lines grouped by product *and* choice, the fallback to priced quantities when that grouping cannot be trusted, catalogue names and photographs snapshotted rather than referenced, `total_cost` summed from `pricing.cost`, a returning customer found by phone with first-touch attribution left alone and name/email refreshed, and that every fault reduces to `FAILED` instead of throwing | [PLAN-order-capture.md](PLAN-order-capture.md) |
| `lib/customer-email-message.test.ts` | The customer order-confirmation email's HTML composition for all three order types — honest subjects and wording, the amount due/paid, items and address, escaping, the cost/margin seal, the order-placed timestamp, the tracking button, and the journey graphic proven to highlight exactly one step ("Order Placed") from its `data-step`/`bgcolor` markup | [PLAN-customer-order-confirmation-email.md](PLAN-customer-order-confirmation-email.md), [ADR-062](../decisions/ADR-062-customer-order-confirmation-email.md) |
| `lib/notify-customer-email.test.ts` | The Resend transport and both orchestration functions — exactly one send attempt, the verified `from` address, silent skips with no key or no address, a reported error and a rejected send both resolving `FAILED` without throwing, the finite timeout, and the `PAID`-only guard on the paid/partial dispatch | [PLAN-customer-order-confirmation-email.md](PLAN-customer-order-confirmation-email.md), [ADR-062](../decisions/ADR-062-customer-order-confirmation-email.md) |
| `lib/order-id.test.ts` | The ten-character order number — the alphabet derived from its own exclusion list, uniform character selection from `node:crypto`, the uniqueness retry, and the bounded failure when the check is broken | [ADR-043](../decisions/ADR-043-order-id-as-primary-identifier.md) |
| `lib/checkout-capture-route.test.ts` | `/api/create-order` end to end with capture in place — that the 200 body carries both ids, and that neither the request shape nor the amount sent to Cashfree moved | [PLAN-order-capture.md](PLAN-order-capture.md) |
| `lib/checkout-database-failure.test.ts` | That a database which is down, slow or refusing changes **no byte** of the checkout response — the load-bearing assertion behind ADR-042's off-the-critical-path claim | [PLAN-order-capture.md](PLAN-order-capture.md) |
| `lib/admin-orders.test.ts` | The order list's query vocabulary — view, status, search, date range, sort and page parsed out of the URL and back into it, and the Prisma `where` each produces | [PLAN-admin-orders-list.md](PLAN-admin-orders-list.md) |
| `lib/admin-orders-access.test.ts` | That the list is reachable only through the protected layout, and what an unauthenticated request gets instead | [PLAN-admin-orders-list.md](PLAN-admin-orders-list.md) |
| `lib/admin-layout-shell.test.tsx` | The panel's own shell and nav — that no storefront chrome travels with an admin screen, and that the signed-in operator is named on every protected page | [ADR-044](../decisions/ADR-044-admin-order-detail-and-layout-split.md) |
| `lib/order-status.test.ts` | The seven statuses and the three payment types by their labels, and that the label list and the enum cannot drift | [PLAN-admin-orders-list.md](PLAN-admin-orders-list.md) |
| `lib/order-transitions.test.ts` | The lifecycle table itself — every allowed edge and, more importantly, every absent one; which statuses demand a reason and a refund decision; the address-editable window; and the two receipt-toggle preconditions | [PLAN-admin-order-detail.md](PLAN-admin-order-detail.md) |
| `lib/order-status-change.test.ts` | One submitted status change validated against the lifecycle and the order's own money — the eight rejection codes, the refund ceiling read from `amount_prepaid` rather than the total, `isRefunded` derived from the amount, and the COD acknowledgement path | [PLAN-admin-order-detail.md](PLAN-admin-order-detail.md) |
| `lib/admin-order-updates.test.ts` | The three writers — the status change and its audit row as one transaction, the concurrency guard that refuses a plan made against a status the order has since left, the address edit that writes an unchanged status with a reason naming the moved fields, and the two independent receipt flags. Also the receipt write's own race, added by ADR-048: a stub client is what makes the window between the precondition read and the write observable at all | [PLAN-admin-order-detail.md](PLAN-admin-order-detail.md), [PLAN-database-failure-surfaces.md](PLAN-database-failure-surfaces.md) |
| `lib/admin-order-action-routes.test.ts` | The three endpoints as endpoints — that each resolves the session against Postgres rather than trusting middleware, that `changedBy` comes from the session and never the body, and the 401/404/409/422 mapping | [PLAN-admin-order-detail.md](PLAN-admin-order-detail.md), [admin-orders-id-status contract](../api/admin-orders-id-status.md) |
| `lib/admin-order-detail.test.ts` | The detail query — order-id normalisation, the shipping address read back out of `Json`, and what the screen is handed | [PLAN-admin-order-detail.md](PLAN-admin-order-detail.md) |
| `lib/admin-order-panels.test.tsx` | The detail screen's panels — items, totals, address, timeline, money, and the receipt toggles appearing only when the order's state makes them meaningful | [PLAN-admin-order-detail.md](PLAN-admin-order-detail.md) |
| `lib/admin-order-status-form.test.tsx` | The status form — that the dropdown offers only valid next steps, and that the reason and refund fields appear exactly when the chosen status demands them | [PLAN-admin-order-detail.md](PLAN-admin-order-detail.md) |
| `lib/analytics.test.tsx` | The GA4 tag — that nothing at all is rendered without a measurement id, that the id is JSON-escaped into the inline `config` call, and that the tag host matches the one the CSP allows | [PLAN-analytics-and-utm.md](PLAN-analytics-and-utm.md) |
| `lib/utm.test.ts` | UTM capture — shape validation, control-character stripping, whitespace collapsing, the 120-character bound, first-touch persistence that a later visit cannot overwrite, and the `order_tags` triple | [PLAN-analytics-and-utm.md](PLAN-analytics-and-utm.md) |
| `lib/utm-ssr.test.ts` | That UTM capture is a client concern and no server render reads it | [PLAN-analytics-and-utm.md](PLAN-analytics-and-utm.md) |
| `lib/payment.test.ts` | `/payment` and the create-order response guard — `isCreateOrderSuccess` accepting a null `trackingId` while rejecting a missing key or an empty string | [ADR-043](../decisions/ADR-043-order-id-as-primary-identifier.md) |
| `lib/health.test.ts` | The database health probe — a live Postgres reported `reachable`, a refused connection reported `unreachable`, an **unmigrated** database reported `schema-mismatch` rather than healthy, a database that accepts the connection and never answers bounded rather than hung, and a report whose key set is exactly three fields with no host, port or driver name in it. The one live case skips with no database | [PLAN-database-failure-surfaces.md](PLAN-database-failure-surfaces.md), [health contract](../api/health.md) |
| `lib/health-database-failure.test.ts` | `GET /api/health` with Postgres refused at the module boundary — 503, `no-store`, and the assertion the route exists for: that it disagrees with the storefront homepage, which renders perfectly well in the same state | [PLAN-database-failure-surfaces.md](PLAN-database-failure-surfaces.md), [ADR-048](../decisions/ADR-048-database-health-and-failure-surfaces.md) |
| `lib/admin-order-database-failure.test.ts` | The three order actions and the login endpoint against a database that refuses everything — 503 `DATABASE_UNAVAILABLE` in the documented shape rather than an unhandled rejection, 500 `SERVER_ERROR` kept distinct so nobody restarts a healthy Postgres, and the real `submitAdminOrderAction` shown rendering the endpoint's own sentence instead of "the server did not say why". The session read is what fails, which is why the boundary wraps it | [PLAN-database-failure-surfaces.md](PLAN-database-failure-surfaces.md), [admin-orders-id-status contract](../api/admin-orders-id-status.md) |
| `lib/admin-login-sweep.test.ts` | A correct login whose expired-session sweep throws — signed in anyway, with the cookie, the failure in the log and none of it in the response, and a wrong password still rejected. The one admin surface ADR-048 decided should degrade silently | [PLAN-database-failure-surfaces.md](PLAN-database-failure-surfaces.md), [admin-login contract](../api/admin-login.md) |
| `lib/admin-page-database-failure.test.tsx` | The protected layout and both admin pages with Postgres unreachable — each renders the panel's own error state rather than Next's generic 500; the list shows **neither** empty-state sentence, the detail page does not call `notFound()`, and the layout neither renders the panel nor redirects, so an outage cannot read as an expired session | [PLAN-database-failure-surfaces.md](PLAN-database-failure-surfaces.md), [ADR-048](../decisions/ADR-048-database-health-and-failure-surfaces.md) |
| `lib/tracking-database-failure.test.tsx` | `/track` with Postgres unreachable — the page's own not-found copy, the form still usable, nothing about the database in the HTML, and a malformed number answered identically to a well-formed one so the outage is not an oracle | [PLAN-database-failure-surfaces.md](PLAN-database-failure-surfaces.md), [PLAN-public-order-tracking.md](PLAN-public-order-tracking.md) |
| `lib/prepare-migration-batch.test.ts` | Stage 0 of the Odoo migration — the eleven-slug category rule and the `gift-hampers` warning, the sub-50-character stub rule, the on-disk image check, the P101 override's safety assertion firing at P050, id assignment proved deterministic under a reversed file order, the Odoo-to-schema variant and image transformation with `verified_distinct` carried as evidence beside the suggestion rather than inside it, and three assertions read against the script's own source with its prose stripped, proving it names none of the extraction skills and never writes the catalogue | [PLAN-stage-0-migration-batch.md](PLAN-stage-0-migration-batch.md), [ADR-054](../decisions/ADR-054-stage-0-migration-batch-preparation.md) |
| `lib/category-vocabulary.test.ts` | The eleventh category — that `gift-hampers` is valid in all four enumerations and that they cannot drift apart, that a draft carrying it passes both Draft A validators, and that it is nonetheless absent from the nav, the sitemap, the `?category=` parser and the catalogue, because a category can be agreed before its products exist | [RESULT-2026-08-23-category-vocabulary.md](RESULT-2026-08-23-category-vocabulary.md), [ADR-055](../decisions/ADR-055-category-vocabulary-and-surfacing.md) |
| `lib/payment-paths.test.ts` | The payment-plan arithmetic — the per-unit floor summed over a cart, an unresolvable cart failing safe to full prepayment, each of the three paths priced, and every refusal: cash on delivery on a barred cart, a part payment with no floor, and a part payment whose floor has reached the total. The `amountPrepaid + amountDue = total` invariant is asserted **as one property over all three paths** across seven totals rather than as three checks of individual fields, and again through `isBalancedOrderPayment`, the guard `captureOrder` runs before the insert. **Plus the online-payment discount** ([ADR-063](../decisions/ADR-063-online-payment-discount.md)): 5% of the subtotal on `"full"` for an eligible cart, never on shipping, never on `"cod"` or a barred cart's either option, never when eligibility is unresolved, and the nearest-rupee rounding proved to match `calculateOnlinePaymentDiscount` exactly across nine subtotals including several landing on `.5` | [PLAN-checkout-payment-paths.md](PLAN-checkout-payment-paths.md), [ADR-059](../decisions/ADR-059-checkout-payment-paths.md), [ADR-063](../decisions/ADR-063-online-payment-discount.md) |
| `lib/checkout-payment-paths.test.ts` | The three paths through the **real route against the real database**. Opens with the regression case that pins the pre-existing behaviour of a body naming no `paymentPath`, which was run green against the unmodified route before anything changed. Then: cash on delivery asserted to call a spied `fetch` **zero times**, its `COD_…` reference refused by `/api/verify-order` before any gateway call, its read-back through `/api/cod-order`, a part payment sending Cashfree the floor rather than the total, every `PAYMENT_PATH_UNAVAILABLE` refusal, and a body carrying six lying amounts reaching none of them. The part-payment cases override `getCodEligibilityCatalogue` rather than editing the catalogue, and **no case names the piece it buys** — the cash-on-delivery carts find the first catalogue entry reading `minPrepaidAmount: 0`, so a floor appearing on any one product cannot quietly turn a COD case into a refusal case. **Plus the online-payment discount, end to end**: `order_amount` sent to the mocked Cashfree `fetch` and the written Postgres row both proved to carry the discounted figure, a body naming its own `discount`/`onlineDiscount`/`amountPrepaid`/`total` proved unable to move either, and the barred-cart exclusion repeated at the route layer — every expected figure derived from the catalogue on disk rather than hardcoded | [PLAN-checkout-payment-paths.md](PLAN-checkout-payment-paths.md), [ADR-059](../decisions/ADR-059-checkout-payment-paths.md), [ADR-063](../decisions/ADR-063-online-payment-discount.md) |
| `lib/payment-checkout.test.tsx` | The payment step in both of its states — cash on delivery beside full payment for an eligible cart, the minimum beside full payment for a barred one, and no choice at all when the floor has reached the total. Asserts the quoted figures, the per-unit multiplication, the honest button label, and that choosing cash on delivery sends `paymentPath: "cod"`, skips the Cashfree SDK entirely and routes straight to the confirmation page with the bundle stamped. **Plus the online-payment discount**: the "Save 5%" badge and discounted pay button on an eligible cart, the live Order Summary discount row that disappears the instant COD is chosen, and the regression that a cart requiring prepayment shows neither | [PLAN-checkout-payment-paths.md](PLAN-checkout-payment-paths.md), [ADR-059](../decisions/ADR-059-checkout-payment-paths.md), [ADR-063](../decisions/ADR-063-online-payment-discount.md) |
| `lib/product-repository.test.ts` | The one door to the catalogue. Reads that go to the file rather than to a cached copy; a valid edit saved and read back; every other record proved byte-identical; the keyword map rebuilt in the same operation; a no-op edit reported `UNCHANGED` without rewriting 1.38 MB; no temporary file left behind. Then the refusals — a record-level rule, a precious-metal claim, a primary keyword another product owns, an edit that would empty a surfaced category — each asserted to write **nothing**, plus the case that a failure the catalogue already had does not make an unrelated product uneditable. Concurrency: a stale token refused, a second edit made between render and save proved not to be clobbered, and two products proved to carry two different tokens | [PLAN-admin-product-management.md](PLAN-admin-product-management.md), [ADR-064](../decisions/ADR-064-admin-product-management.md) |
| `lib/product-repository-boundary.test.ts` | **The test that makes the repository boundary real rather than a promise in a review.** Enumerates both route directories recursively plus twelve named modules and asserts none imports `@/data/products.json`, `@/lib/products` or `@/lib/shop` — matched as import *specifiers*, so the doc comments that mention `lib/products.ts` are correctly not violations — that the pages and route positively go **through** `productRepository`, that only the JSON implementation touches `node:fs/promises`, and that nothing else in the feature writes a file. Plus the margin seal: a grep over real `.next/static` chunks for a product id beside `"cost"`, on the [TC-31](RESULT-2026-08-20-order-capture.md) grep-the-build method, skipped when no production build is present | [PLAN-admin-product-management.md](PLAN-admin-product-management.md), [ADR-064](../decisions/ADR-064-admin-product-management.md), [ADR-040](../decisions/ADR-040-postgres-for-orders.md) |
| `lib/product-validation.test.ts` | That the panel is held to the build's rules and not to a friendlier subset. All 449 shipped records proved to pass; eleven record-level rules and four catalogue-level ones that a single record cannot see (duplicate keyword, duplicate meta title, duplicate id, a merchandising row emptied by unfeaturing); the baseline diff that attributes a failure to the edit which caused it, including a floor breach that names no product id at all. Closes with the structural assertion that the gate imports `scripts/product-record-rules.mjs` and **retains no validate function of its own** | [PLAN-admin-product-management.md](PLAN-admin-product-management.md), [ADR-064](../decisions/ADR-064-admin-product-management.md) |
| `lib/admin-products.test.ts` | The product list's query language — the four views that partition the catalogue, the category/flag filters, the four sorts, search, pagination and the URL vocabulary. Pure: it reads a `Product` and a query and touches no filesystem | [PLAN-admin-product-management.md](PLAN-admin-product-management.md), [ADR-064](../decisions/ADR-064-admin-product-management.md) |
| `lib/admin-product-routes.test.ts` | `PATCH /admin/api/products/{id}` against the real handler. Where the route lives on both hostnames and that middleware gates it; a 401 that never opens the catalogue and a body that cannot claim an identity; all six outcome mappings (200 `UPDATED`, 200 `UNCHANGED`, 404, 422 listing every broken rule, 409, 503); and the trust boundary — the URL decides which product is edited, a non-JSON body is refused rather than fatal, a hostile shape is coerced, and **values are passed through untouched for the real rules to judge** | [PLAN-admin-product-management.md](PLAN-admin-product-management.md), [admin-products-id.md](../api/admin-products-id.md) |
| `lib/admin-product-form.test.tsx` | The three-tab editor. Every one of the 449 records proved to survive `toProductDraft` → `toProductEdit` unchanged, SEO block included; a blank amount proved to become something the validator refuses rather than a zero; a non-integer passed through rather than rounded; variant rows derived from the draft's own options, with a stranded image forgotten. Then the behaviours: edits kept across tab switches, one `PATCH` carrying all three tabs, the returned version adopted so a second save is not a false conflict, a refused save showing every rule in the build's words while keeping the operator's typing, and a read-only deployment disabling the save rather than accepting an edit it would discard | [PLAN-admin-product-management.md](PLAN-admin-product-management.md), [ADR-064](../decisions/ADR-064-admin-product-management.md) |
| `lib/admin-product-pages.test.tsx` | Both screens against the real catalogue — a full first page with the true total, every row linked, per-tab counts matching what each view holds, category and out-of-stock filtering, an empty state that talks about the catalogue rather than an error, and a `draft` product opening where the storefront would refuse it. Closes on the failure surface: an unreadable catalogue renders `AdminCatalogueError` on both screens and is asserted **not** to be worded as a database outage | [PLAN-admin-product-management.md](PLAN-admin-product-management.md), [ADR-048](../decisions/ADR-048-database-health-and-failure-surfaces.md) |
