# Architecture Decision Records

## Purpose

An ADR records one architectural decision: what was chosen, what the alternatives were, and
why the choice was made. It captures the reasoning that the code itself cannot show — six
months from now the code says *what* we do, and these files say *why*.

Write an ADR when a choice is expensive to reverse or would otherwise be re-litigated:
frameworks, payment provider, data storage, hosting, auth model, state management, the
shape of a public contract. Do not write one for routine implementation detail.

## Lifecycle

An accepted ADR is immutable. Do not rewrite history when a decision changes — write a new
ADR that supersedes the old one, then edit the old one's status line to
`Superseded by ADR-NNN` and leave the rest of its body untouched.

Valid statuses: `Proposed`, `Accepted`, `Superseded by ADR-NNN`, `Deprecated`.

## Naming convention

```
ADR-NNN-short-kebab-case-title.md
```

`NNN` is a zero-padded sequential number starting at `001`, never reused. Example:
`ADR-001-tech-stack.md`, `ADR-002-cart-state-persistence.md`.

## Required structure

```markdown
# ADR-NNN: Title

- **Status:** Accepted
- **Date:** YYYY-MM-DD
- **Prompt:** N

## Context
The situation and constraints that forced a decision.

## Decision
What we are doing, stated plainly.

## Alternatives considered
Each option with why it was rejected.

## Consequences
What this makes easy, what it makes hard, and what would force us to revisit it.
```

## Index

| ADR | Title | Status |
| --- | --- | --- |
| [001](ADR-001-tech-stack.md) | Tech stack | Accepted |
| [002](ADR-002-product-data-model.md) | Product data model | Accepted — record shape superseded by [027](ADR-027-product-schema-migration.md) |
| [003](ADR-003-discount-display-pricing.md) | Discount display pricing | Accepted |
| [004](ADR-004-design-system.md) | Design system | Accepted |
| [005](ADR-005-navigation-and-chrome.md) | Navigation and global chrome | Accepted — announcement strip superseded by [028](ADR-028-header-restructure.md) |
| [006](ADR-006-product-image-convention.md) | Product image convention and placeholders | Accepted |
| [007](ADR-007-home-composition.md) | Home page composition | Accepted |
| [008](ADR-008-shop-architecture.md) | Shop page architecture | Accepted |
| [009](ADR-009-product-page.md) | Product detail page | Accepted |
| [010](ADR-010-cart-architecture.md) | Cart architecture | Accepted |
| [011](ADR-011-checkout-address-step.md) | Checkout step 1 — the address page | Accepted |
| [012](ADR-012-static-and-policy-pages.md) | Static content and the policy set | Accepted — `PolicyDisclaimer` superseded by [037](ADR-037-policy-disclaimer-removal.md) |
| [013](ADR-013-order-creation-and-payment.md) | Order creation and the payment step | Accepted |
| 014 | *Never written — see Numbering gaps below* | — |
| [015](ADR-015-business-config-and-shipping-threshold.md) | Single-source business config and a free-shipping threshold | Accepted |
| [016](ADR-016-real-product-import.md) | Real product import and the P-code id scheme | Accepted |
| [017](ADR-017-final-content-pass.md) | Final content pass on the policy set and the about page | Accepted |
| [018](ADR-018-honest-product-description.md) | Honest product description — anti-tarnish, not hallmarked | Accepted |
| [019](ADR-019-product-options.md) | Product options — a recorded choice, never a price | Accepted |
| [020](ADR-020-two-tier-catalogue-ia.md) | Two-tier catalogue IA — categories and collections | Accepted |
| [021](ADR-021-all-real-catalogue.md) | An all-real catalogue and the end of placeholder products | Accepted |
| [022](ADR-022-logo-integration.md) | The real logo replaces the text wordmark | Accepted |
| [023](ADR-023-home-polish.md) | Home polish — real imagery, everyday positioning, button spacing | Accepted |
| [024](ADR-024-funnel-ui-polish.md) | Funnel UI polish — logo scale, two button scales, card alignment, one price facet, compact product details, em-dash sweep | Accepted |
| [025](ADR-025-button-padding-tailwind-content.md) | Button padding never rendered — `lib/` was outside Tailwind's content globs | Accepted |
| [026](ADR-026-paired-cta-equal-width.md) | A pair of calls to action is one grid, not two buttons | Accepted |
| [027](ADR-027-product-schema-migration.md) | Product record grouped by purpose, four named option controls, per-variant images and a multi-image gallery | Accepted |
| [028](ADR-028-header-restructure.md) | Two header bands, not three — the announcement moves into the logo row | Accepted |
| [029](ADR-029-seo-foundations.md) | SEO foundations — a generated sitemap, robots rules, and structured data that repeats the real policies | Accepted |
| [030](ADR-030-dependency-security-bump.md) | Dependency security — what a patch bump could fix, and why Next.js was not one of them | Accepted |
| [031](ADR-031-mobile-scale.md) | A mobile scale of its own, added under the desktop layout rather than beside it | Accepted |
| [032](ADR-032-coolify-docker-deploy.md) | Containerised deploy on Coolify — standalone output and a three-stage Dockerfile | Accepted — narrows the hosting row of [001](ADR-001-tech-stack.md) |
| [033](ADR-033-mobile-layout-round-two.md) | Four mobile layouts that differ in kind from their desktop counterparts, not just in scale | Accepted |
| [034](ADR-034-seo-audit-remediation.md) | SEO audit remediation — fabricated reviews removed, security headers added | Accepted — retires the `aggregateRating` and `review` of [029](ADR-029-seo-foundations.md) and the `amber` star token of [004](ADR-004-design-system.md) |
| [035](ADR-035-catalogue-content-pass.md) | The catalogue content pass — approved copy in, false claims out, missing values deferred to the owner | Accepted — extends the honesty sweep of [018](ADR-018-honest-product-description.md) into `data/products.json` |
| [036](ADR-036-product-seo-metadata-pass.md) | Per-product search and social metadata, written into the catalogue rather than derived from the description | Accepted — retires `buildProductMetaDescription` and decision 3 of [035](ADR-035-catalogue-content-pass.md). **Addendum 2026-08-23:** the site-wide keyword map its collision rule is enforced against now exists at `data/keyword-map.json`, derived from the catalogue and staleness-checked in the gate; no hard collision among the 49 |
| [037](ADR-037-policy-disclaimer-removal.md) | Policy pages stop disclaiming themselves | Accepted |
| [038](ADR-038-dead-code-and-doc-accuracy-cleanup.md) | Verified-dead code removed, and the documentation corrected to match the repository | Accepted |
| [039](ADR-039-analytics-and-utm-attribution.md) | GA4 as the analytics tool, and first-touch UTM attribution stored in the browser | Accepted — widens the CSP of [034](ADR-034-seo-audit-remediation.md) by three Google origins |
| [040](ADR-040-postgres-for-orders.md) | Postgres for orders and CRM, alongside the JSON catalogue rather than replacing it | Accepted — narrows the no-database row of [001](ADR-001-tech-stack.md); the catalogue stays in `data/products.json`. **Addendum (prompt 44):** a terminal status is the `OrderStatus` enum and nothing else — no mirroring booleans or timestamps; `pricing.cost` joins the catalogue as server-only margin data |
| [041](ADR-041-admin-subdomain-and-auth.md) | The admin panel is a subdomain of one deployment, behind a database-backed session | Accepted — introduces an authenticated operator, as [040](ADR-040-postgres-for-orders.md) said it would. **DNS and Coolify wiring for `admin.morchadigems.com` are explicitly deferred** to a later deployment prompt. **Credential storage superseded by [061](ADR-061-env-var-admin-credentials.md)** — session storage, routing and the two-layer gate are unaffected |
| [042](ADR-042-order-capture-in-postgres.md) | Orders are captured in Postgres at checkout, and the write is not allowed to break checkout | Accepted — the first real traffic to write to the tables [040](ADR-040-postgres-for-orders.md) created. Adds the payment-type fields **without** any customer-facing COD choice, and makes the database write off-critical-path on the [notify-admin](../api/notify-admin.md) precedent |
| [043](ADR-043-order-id-as-primary-identifier.md) | The ten-character order id is the order's public name, and the create-order response names both ids | Accepted — makes the id [040](ADR-040-postgres-for-orders.md) shaped and [042](ADR-042-order-capture-in-postgres.md) started minting the one a shopper is shown. Renames the create-order response's `orderId` to `cashfreeOrderId` and adds a nullable `trackingId` |
| [044](ADR-044-admin-order-detail-and-layout-split.md) | The order detail screen, and the layout split that let it have one | Accepted — states the order lifecycle once and enforces it in the UI **and** the route handler, forces a reason and a refund decision into the same transaction as an `rto`/`returned`/`cancelled` change, opens the address only before dispatch, and moves the storefront into `app/(storefront)` so the panel stops inheriting the shop's header, footer and WhatsApp button |
| [045](ADR-045-public-order-tracking.md) | Public order tracking — the order number is the whole credential, and the page is told almost nothing | Accepted, **narrowed by [071](ADR-071-order-tracking-detail-and-timestamps.md)** — which adds the order's items and a payment summary and re-refuses the address on this ADR's own reasoning. Clears the `sessionStorage` limitation [043](ADR-043-order-id-as-primary-identifier.md) shipped with, and draws the customer-facing half of the boundary [044](ADR-044-admin-order-detail-and-layout-split.md) drew for the operator |
| [046](ADR-046-saved-address-in-local-storage.md) | The browser remembers the delivery address; the shop still does not remember the shopper | Accepted — removes repeat-checkout friction without reopening the no-accounts row of [001](ADR-001-tech-stack.md) |
| [047](ADR-047-prisma-generate-in-docker-build.md) | The production image generates the Prisma Client itself | Accepted — narrows the *no database* premise [032](ADR-032-coolify-docker-deploy.md) was written under, and delivers the first of the two build steps [040](ADR-040-postgres-for-orders.md) deferred |
| [048](ADR-048-database-health-and-failure-surfaces.md) | Database health is made visible, and each surface decides for itself how to fail | Accepted — closes the silent-failure gap [042](ADR-042-order-capture-in-postgres.md) created by design, and decides the second of the two steps [040](ADR-040-postgres-for-orders.md) deferred by leaving it manual and visible rather than automated |
| [049](ADR-049-next-14-advisory-triage-and-upgrade-scope.md) | The 21 Next.js advisories, re-triaged against an app that now has middleware — and the scope of the upgrade that would clear them | Accepted — re-runs the exposure triage [030](ADR-030-dependency-security-bump.md) performed under a *no middleware* premise that [041](ADR-041-admin-subdomain-and-auth.md) ended. **Assessment only — no dependency version changed.** 2 of 21 reachable today, 3 more latent behind one Cloudflare setting; rejects the offered `prisma@6.12.0` downgrade; recommends 15.5.23 over 16.3.2. **Addendum (prompt 58):** two of its three interim mitigations actioned — the *Cache Everything* prohibition is in [DEPLOY.md](../../DEPLOY.md) §4, and `images.qualities: [75]` cuts the image cache's reachable variant space by 99% |
| [050](ADR-050-unified-gallery-strip.md) | One thumbnail strip for every photograph, and clicking one records its choice | Accepted — supersedes in part the gallery behaviour of [027](ADR-027-product-schema-migration.md) and [036](ADR-036-product-seo-metadata-pass.md); `media.variantImages` joins the strip instead of being reachable only through the option selector |
| [051](ADR-051-draft-a-content-pipeline.md) | The Draft A content pipeline — two intake paths, one schema, and the allow-lists that gate them | Accepted — **design record only.** Extends the P-code scheme of [016](ADR-016-real-product-import.md) with permanent id reservation, and turns the candidate lists of prompt 63 into a stated requirement for two owner-curated allow-lists. **Addendum (prompt 71):** the allow-list gate is retired — every candidate goes to owner review regardless of any curated file, so `data/material-phrases.json` is no longer a prerequisite and `data/stone-terms.json` is an optional lookup whose absence downgrades a stone to `unverified-guess`. `scripts/validate-draft-a.mjs` **is built**, with 100 tests. `data/stone-terms.json` has since been seeded (2026-08-24, first entry `American Diamond` → `cubic zirconia`); the phase-three similarity calibration has produced measurements but set no threshold — the gate stays advisory. **Addendum (prompt 75):** rule A3 is about confirmation rather than absence — see [056](ADR-056-image-confirmation-provenance-and-draft-similarity.md) |
| [052](ADR-052-product-status-field.md) | A publication status on the product record, enforced at one chokepoint | Accepted — adds `status` to the record shape of [027](ADR-027-product-schema-migration.md); gives the unapproved records of [051](ADR-051-draft-a-content-pipeline.md) somewhere to live in `data/products.json` without a shopper reaching them. Filtered once in `lib/products.ts` rather than at each of the fourteen surfaces that read the catalogue |
| [053](ADR-053-draft-a-to-product-orchestration.md) | Phase 2 — the Draft A to product orchestration, its attribute mapping, and an advisory-only similarity gate | Accepted — builds the Phase 2 pipeline [051](ADR-051-draft-a-content-pipeline.md) left undesigned; writes records in the `draft` state of [052](ADR-052-product-status-field.md); reads the keyword map behind [036](ADR-036-product-seo-metadata-pass.md) |
| [054](ADR-054-stage-0-migration-batch-preparation.md) | Stage 0 — deterministic migration batch preparation, and why it stops before extraction | Accepted — puts a validate-and-assign step in front of the Draft A pipeline of [051](ADR-051-draft-a-content-pipeline.md), whose id-reservation rule it reconciles rather than replaces. Assigns migrated ids from **P101**, retiring P050–P100; suggests image paths in the conventions of [006](ADR-006-product-image-convention.md); accepts a `gift-hampers` category that the ten of [020](ADR-020-two-tier-catalogue-ia.md) do not contain — queued with a warning and an open owner decision, **now closed** by [055](ADR-055-category-vocabulary-and-surfacing.md) — see this record's addendum |
| [055](ADR-055-category-vocabulary-and-surfacing.md) | `gift-hampers` is the eleventh category, and a category's vocabulary is separated from its shopfront | Accepted — closes the open question of [054](ADR-054-stage-0-migration-batch-preparation.md) and widens the ten-category tier of [020](ADR-020-two-tier-catalogue-ia.md) to eleven. Applies the hidden-but-valid state of [052](ADR-052-product-status-field.md) one tier up: `CATEGORIES` is the vocabulary, `SURFACED_CATEGORIES` is what a shopper sees, and `validate-products.mjs` checks the gap in both directions. A static flag rather than a catalogue count, for the client-bundle reason of [010](ADR-010-cart-architecture.md) |
| [056](ADR-056-image-confirmation-provenance-and-draft-similarity.md) | An image suggestion is confirmed like an attribute, a migrated record keeps its origin, and the similarity gate can see the batch it is scoring | Accepted — three fixes from the [pre-migration readiness audit](../testing/RESULT-2026-08-23-pre-migration-readiness-audit.md), all one shape of mistake: a mechanism built for 49 hand-written products, never extended to bulk-migrated data. Turns rule A3 of [051](ADR-051-draft-a-content-pipeline.md) from *images must be empty* into *no image may already be confirmed*, mirroring its own attribute rule; **reverses decision 5 of [054](ADR-054-stage-0-migration-batch-preparation.md)** by moving `verifiedDistinct` inside the suggestion, which is what lets it cross extraction; adds `subcategory` and a nested server-only `migrationProvenance` to the record shape of [027](ADR-027-product-schema-migration.md), sealed like the `pricing.cost` of [040](ADR-040-postgres-for-orders.md) and **verified by grepping a real build**; and applies decision 4 of [053](ADR-053-draft-a-to-product-orchestration.md) to descriptions, so the similarity gate scores drafts against each other. `SIMILARITY_THRESHOLD` stays `null` — nothing is refused |
| [057](ADR-057-staging-colocation-and-completed-tracking.md) | Staging co-location, publish-time archival, and tracking `completed/` in git | Accepted — the owner-approved restructure from the [post-pilot pipeline audit](../testing/RESULT-2026-08-24-pipeline-audit-post-pilot.md) Part D. Merges each `odoo-{id}/` image directory into its product's `PNNN/` staging directory (one directory per product, `sourceFile` strings rewritten — safe because nothing reads them after Stage 0); extends the publish step of [052](ADR-052-product-status-field.md)/[053](ADR-053-draft-a-to-product-orchestration.md) to archive the whole staging directory into `completed/PNNN/`, so `incoming/` empties as products ship; and settles the tracking question left open by [051](ADR-051-draft-a-content-pipeline.md): **`completed/` is tracked in git** — a product's source images and provenance enter history at the moment of publish — while `incoming/` and `drafts/` stay untracked. Adds the read-only `npm run report:images` (duplicate-hash groups, confirmation counts, missing files, orphans); builds no copy helper and sets no new state |
| [058](ADR-058-cod-eligibility-and-min-prepaid-amount.md) | Cash on delivery is decided per product by `pricing.minPrepaidAmount`, and a cart qualifies only unanimously | Accepted — Cashfree denied COD at the account level (ticket 8266236), supplying no pincode, RTO or order-value screening, so COD is built storefront-side with the owner explicitly accepting the unscreened risk. Adds `minPrepaidAmount` (per unit, whole rupees) to the `pricing` block of [027](ADR-027-product-schema-migration.md); `0` means COD-eligible and all 449 records were backfilled to it. `isCartCodEligible` offers COD only when **every** line reads `0` — a mixed cart is refused rather than split into a prepaid and a collected part, which is a reconciliation problem — and an **empty cart is explicitly ineligible** rather than vacuously true. Eligibility gets a **fourth catalogue accessor** rather than a wider `OrderPricingEntry`, on the reasoning that gave `pricing.cost` its own in [040](ADR-040-postgres-for-orders.md): the seal cuts both ways, so the pricing core cannot read `minPrepaidAmount` into a total and the eligibility rule cannot read `price` into a hidden order-value threshold. Migrated records arrive eligible — a draft does not get to assert the shop's exposure. Validator split mirrors `cost`: shape hard, amount-above-price advisory. **Data model, validation and pure logic only** — the checkout UI, the COD order path and `captureOrder`'s `paymentType` are deliberately a later prompt |
| [059](ADR-059-checkout-payment-paths.md) | Checkout offers three payment paths, and the server decides what each one costs | Accepted — the work [058](ADR-058-cod-eligibility-and-min-prepaid-amount.md) deliberately deferred. The client sends a **word** (`"cod"`, `"partial"`, `"full"`; absent reads as `"full"`, so every pre-existing body is unchanged) and `resolvePaymentPlan` prices it from the server's own total and its own summed prepayment floor; a path the cart does not permit is **refused**, never downgraded. `amountPrepaid + amountDue = total` is re-checked by `captureOrder` before the insert. **The Prisma enum did not change and no migration was written** — `prepaid`/`cod`/`partial_cod` from [042](ADR-042-order-capture-in-postgres.md) is already exactly right, so the proposed rename's live-data risk does not arise. COD skips Cashfree entirely and carries a locally minted `COD_…` payment reference, deliberately **not** in the `MG_…` shape, so `isMorchadiOrderId` rejects it and `/api/verify-order` cannot be led into rendering Cashfree's inevitable 404 as "nothing has been charged" over a real order. **A failed capture is fatal for COD and only for COD**: [042](ADR-042-order-capture-in-postgres.md)'s tolerance rests on the money being recoverable from Cashfree, and an unwritten COD order exists nowhere. New sibling route `/api/cod-order` reads it back so the confirmation survives a refresh. The known gap left open: **the owner gets no WhatsApp for a COD order**, because `/api/notify-admin` establishes warrant by asking Cashfree |
| [060](ADR-060-cod-order-notification.md) | A cash-on-delivery order notifies the owner from the route that wrote it, and the write is the warrant | Accepted — closes the gap [059](ADR-059-checkout-payment-paths.md) left open. `/api/notify-admin` establishes warrant by asking Cashfree whether an order was really paid, which is the only thing keeping it from being an open messaging relay, and a COD order has no payment to ask about. **The alternative of teaching that route a second, Postgres-read warrant for `COD_…` references was rejected**: it would re-verify what the calling code already knows, double the surface of an endpoint whose safety rests on being narrow, make COD messaging replayable by anyone holding their own reference, and hand the trigger back to a browser that has no verified payment result. Instead `/api/create-order` calls `notifyOwnerOfCodOrder` from the branch that captured the order — the committed row *is* the verification, and a stronger one than a third party's answer. **Not awaited** (`void`), because the shopper is blocked on this response where the paid case's shopper is not. `composeCodOrderMessage` is a sibling of the paid composer, not a flag on it: no `*Paid:*` line exists to misread, `*Due on delivery:*` carries the only prominent figure, and the closing line asks for cash to be collected rather than pointing at a Cashfree dashboard holding nothing. `sendOwnerWhatsApp` is extracted so both warrants share one send, one 5s timeout and one no-retry rule; **`dispatchAdminNotification`'s `PAID` guard is unmoved and no prepaid path changed**. `partial_cod` was confirmed to notify already through the unchanged Cashfree-verified route; that its message omits the balance owed is recorded here as a separate open gap |
| [061](ADR-061-env-var-admin-credentials.md) | Admin credentials move from a Postgres row to environment variables | Accepted — supersedes [041](ADR-041-admin-subdomain-and-auth.md)'s credential storage only; sessions stay a Postgres table. `authenticateAdmin` compares against `ADMIN_USERNAME`/`ADMIN_PASSWORD` in constant time instead of a bcrypt hash in the dropped `Admin` table, fails closed when either is unset, and preserves the byte-identical failure message and 600 ms failure floor exactly. `AdminSession.adminId` becomes a plain string (`ADMIN_IDENTITY_ID`, `"env-admin"`) rather than a foreign key. `scripts/seed-admin.mjs` becomes dead code, kept for reference only. Traded for recovery that does not depend on the SSH tunnel to production, which had proven unreliable |
| [062](ADR-062-customer-order-confirmation-email.md) | A customer order-confirmation email, sent from the two places the owner's WhatsApp already is | Accepted — new `lib/notify-customer-email.ts` (transport) and `lib/customer-email-message.ts` (composition) via Resend, mirroring `lib/notify.ts`/`lib/notify-message.ts` as two functions rather than one unified composer. `sendCodOrderConfirmationEmail` shares [060](ADR-060-cod-order-notification.md)'s write-is-the-warrant COD trigger; `dispatchOrderConfirmationEmail` reuses [notify-admin](../api/notify-admin.md)'s Cashfree-verified `PAID` warrant rather than building a second one. **Closes, in this new channel only,** the partial-payment balance-due gap [060](ADR-060-cod-order-notification.md) recorded and left open in the WhatsApp message. **Addendum (prompt 106):** the plain paragraph shell replaced with a branded, table-based template — accent header with the real logo, a static order-journey graphic, a bordered payment box, a dark footer band — every colour/font a literal restated from `tailwind.config.ts`/`app/layout.tsx`; both composers gain `createdAt: Date \| null` from `orders.created_at`, newly read by `lib/order-capture.ts`. No trigger, warrant or wording decision above changed |
| [063](ADR-063-online-payment-discount.md) | A 5% online-payment discount, computed once and shared by the preview and the charge | Accepted — `resolvePaymentPlan`'s `"full"` branch in `lib/cod.ts` discounts `cart.subtotal` (never shipping, never `mrp`) by `ONLINE_PAYMENT_DISCOUNT_RATE` (5%, nearest rupee) whenever `cart.summary.isCodEligible` reads positively `true`, reusing [058](ADR-058-cod-eligibility-and-min-prepaid-amount.md)'s unanimity gate rather than building a second eligibility concept — a cart holding any piece with `minPrepaidAmount > 0` is excluded structurally, on the `"full"` path exactly as on `"partial"`. `PaymentPlan` gains `total`/`onlineDiscount`; `resolvePaymentPlan`'s parameters change from `{ total, summary }` to `{ subtotal, shipping, summary }` so it can discount one and not the other. `PaymentCheckout.tsx`'s live preview calls the same `calculateOnlinePaymentDiscount` the route does, so the two figures cannot drift |
| [064](ADR-064-admin-product-management.md) | The catalogue gets an editor, behind a repository boundary and a writability gate | Accepted — the first surface to **write** `data/products.json`, and the first narrowing of the *not a catalogue admin* clause of [041](ADR-041-admin-subdomain-and-auth.md); the catalogue row of [001](ADR-001-tech-stack.md) is **unchanged** — publishing is still a commit and a redeploy. Measured, not assumed, that a production write is a **silent no-op**: `lib/products.ts` imports the catalogue statically, so webpack inlines all 449 records into the server bundle and the `data/products.json` in the image is never read. Hence `CATALOGUE_WRITES_ENABLED` — writes on under `next dev`, refused with a 503 in the container, opt-in anywhere else — and `CataloguePublishNotice`, which says which of the two the operator is on. Every surface reaches the catalogue through the new `ProductRepository` (`lib/products.ts` untouched), enforced by `product-repository-boundary.test.ts` rather than by convention, so a `PrismaProductRepository` is the whole of the eventual move. `scripts/validate-products.mjs` was **refactored into** `scripts/product-record-rules.mjs`, which the panel imports too — one implementation of *what makes a record legal*, so an edit cannot pass a weaker check than the build's; only the 1.6M-pair near-match keyword advisory is excluded, and it blocks nothing in either place. Concurrency is a **per-record** truncated SHA-256 version token (per-file would report a conflict between operators editing unrelated products) plus an atomic write-and-`rename`, which is the risk actually worth engineering against — a container stopped mid-write truncates every product, not the one being edited. Adds a row to [048](ADR-048-database-health-and-failure-surfaces.md) via `AdminCatalogueError`. **Confirmed absent from the schema and therefore absent from the UI:** per-variant pricing and naming — `ProductOption` stays `{name, type, values, default}` per [019](ADR-019-product-options.md), and Tab 2 says so on screen |
| [065](ADR-065-admin-sidebar-export-and-variant-picker.md) | The panel gets a sidebar, an export button and a photograph picker | Accepted — a **UX layer over [064](ADR-064-admin-product-management.md)**: the save endpoint, the `ProductEdit` body, the version token, the CONCURRENT_CHANGE refusal and the writes-disabled gate are unchanged, and this is their presentation. The top nav becomes a left sidebar that is still a **Server Component** — the current section still comes from middleware's `x-admin-internal-path`, `usePathname` is still not introduced, and navigating the panel still costs no JavaScript; sections are declared once in `resolveAdminSectionLinks`, so a third is three lines. `scripts/export-live-products.mjs` is **deleted** and becomes `GET /admin/api/products/export`, with `xlsx` a real dependency; it exports **the currently filtered list, every page of it**, through the same `selectMatchingAdminProducts` the list slices, with the count in the button's own label — the unfiltered export is byte-identical to the script's sheet and keeps its filename. Variant photographs stop being typed paths and become a **thumbnail picker**, over `buildGalleryImages` rather than `media.images`: measured first, and **all seven mappings in the catalogue point at files outside `media.images`**, so a picker over that array alone would have shown every real mapping as unassigned and deleted them on the first save. Option values become one field each with the default a `<select>` over them, because every value is now a picker row keyed by its exact text and a textarea's stray blank line is invisible. Fixes a latent [064](ADR-064-admin-product-management.md) bug: `toProductEdit` now derives `variantImages` from the options, so deleting a value no longer saves a mapping for a value the record does not offer. **Unchanged:** per-variant pricing, image upload, product creation and bulk editing are all still out of scope, and `Product`/`ProductOption` are untouched |
| [066](ADR-066-single-source-site-identity.md) | Brand, contact and policy values get one source of truth, enforced by a grep | Accepted — the central config file the brief asked for **already existed**: `config/business.ts` plus its derivations in `lib/config.ts` have held the business facts since [012](ADR-012-static-and-policy-pages.md), so this **extends that pair** rather than adding a competing `lib/site-config.ts`. Thirteen surfaces wrote `"Morchadi Gems"` as a literal (the wordmark's `alt`, `aria-label` and lockup, the admin title template, sidebar and login eyebrow, four funnel meta descriptions, the contact form's default subject, the confirmation email's `From:` display name, the style-guide specimen, two section headings), eight `.mjs` scripts printed it as a banner, and twelve **test files** wrote a contact detail or the name — the real phone number sat in three CallMeBot fixtures beside a fake API key. The one genuine numeric duplicate, `const FREE_SHIPPING_THRESHOLD = 799` in `scripts/product-record-rules.mjs`, was **correct and load-bearing**: the gate must run as bare `node` and cannot import TypeScript. Hence `config/site-facts.mjs` — plain ESM both runtimes read, the same move `security-headers.mjs` makes for `next.config.mjs` — holding the brand names and the three policy numbers; `config/business.ts` republishes the names so it stays the file to open, and `lib/config.ts` re-exports the numbers so **no consumer changed an import**. The admin hostname fallback and the Resend sending mailbox move in as `ADMIN_CONFIG.hostname` and `CONTACT_CONFIG.transactionalFromAddress`; `ADMIN_HOSTNAME` still overrides at runtime, so [041](ADR-041-admin-subdomain-and-auth.md) is unchanged. The wordmark's two-tone split is **stated, not computed** — where the emphasis falls is a design decision, and a derived `split(" ")` would go silently wrong on a one-word rename. **Doc comments may name a value; code may not** — the grep strips comments first, so `middleware.ts` can keep explaining the rewrite by naming the host it rewrites. `lib/site-identity.test.ts` fails on any duplicate; `lib/free-shipping-threshold-propagation.test.tsx` mocks the threshold to 1499 and proves the calculation *and* six copy surfaces move together, which is the half a grep cannot show. `CLAUDE.md` gains the standing rule. **Unchanged:** the catalogue is still `data/products.json` and still ships as code — no row of [001](ADR-001-tech-stack.md) is narrowed |
| [067](ADR-067-card-variant-selection.md) | How a product card asks for an option, and how a cart line changes one | **Superseded in part by [073](ADR-073-universal-add-to-cart-modal.md)** — §1, §2 and §4 only; §3 (editable cart lines) and §5 (`isStockAvailable` and the badge cascade) stand. Originally accepted — the card's Add to cart sent every option group's declared `default` and rendered no control, so a line reached the cart carrying a selection nobody made. **`/api/create-order` could not catch it**: the value was always legal and currently offered, and "the default because the shopper chose it" is the same request on the wire as "the default because nobody was asked" — 42 bangles and 3 rings sent the smallest size, P398 sent `February Purple` to everyone, P408 sent `B`. `selectCardPurchaseMode` is the one decider: `add` (390 products), `choose-on-card` (one group of at most `CARD_OPTION_VALUE_LIMIT` = 3, 43 products, chips with the default pre-selected), `choose-on-page` (16 products, the button reads **"Choose Your Options"** and adds nothing). The three-value ceiling is **a layout constraint, not a judgement about the option**. §2 records that the pre-selected default inside `choose-on-card` is a **known accepted residual risk** — the owner's rule assumed anything high-stakes has more than three values and this catalogue does not match that, so 38 size products and one birthstone pendant keep a *visible* default; the remedy is one line. `isStockAvailable` is the **only** place `stock.inStock` and the new `stock.quantity` combine, and `selectProductBadge` returns at most one badge — Sold Out → Only N left (`LOW_STOCK_THRESHOLD` = 2) → `flags.badge` → `flags.isNew`. **`isNew` was kept and `badge` added beside it**: `isNew` is a *membership* flag driving the new-arrivals row, the collection facet and a catalogue floor and 408 of 449 carry it, so it could never be a badge alone; every New badge that rendered before renders after and **no record needed a backfill**. `changeCartItemOptions` calls **the same `validateOrderLineOptions` the order route runs**, not a copy, so a selection the cart accepts is one checkout accepts; an edit landing on an existing line merges and sums quantities. Alignment is three reserved boxes — name `min-h-[2.75rem]`, chips `h-8`, action `h-11` — which cost `buttonClasses` an opt-in `fillHeight`, since [025](ADR-025-button-padding-tailwind-content.md)'s padding-defines-height rule is exactly what would give two labels two heights. Verified in a browser at five widths ([RESULT-2026-08-31](../testing/RESULT-2026-08-31-card-variant-selection.md)). **Unchanged:** per-variant pricing and the product page's own selector |
| [068](ADR-068-shop-sort-status-and-price-facets.md) | The shop's four sorts, its status facet, and its price buckets | Accepted — both the sorts and the price bands were chosen before the catalogue had 449 products and two had stopped meaning anything. **"Newest first" was not an order**: no timestamp exists — the absence [034](ADR-034-seo-audit-remediation.md) records for ratings — so it sorted on `flags.isNew`, which 408 of 449 records carry, putting nine products in ten in one block and calling it recency; **and it was the default**. **The bands were built for a different price list**: `under-999`/`1000-4999`/`5000-plus` over real prices of ₹35–₹2,999 with 444 of 449 under ₹999. Now four sorts, all properties the catalogue holds, with `newest` gone from `SortSlug`, `SORT_OPTIONS` and `sortComparators` and a stale `?sort=newest` falling back like any unknown token. **The default is `name-asc`** because alphabetical is the one default that says nothing about which pieces the shop wants sold, and because it is stable under a catalogue edit — which is what `buildCanonicalShopHref` needs when sort is stripped from the canonical URL; `localeCompare` keeps casing from splitting the alphabet and every comparator still ends on `id` so pagination cannot drop or repeat. Five **nested** price buckets rather than partitioned, because a shopper with a budget wants one question answered, not to work out which disjoint band they straddle; nesting is safe since selections inside a facet are OR-ed, and `above-999` starts at 1000 so no rupee is uncovered at exactly 999. The typed range is a **separate facet, AND-ed**, and an **inverted range is dropped whole** — a `min` above its `max` describes no price, and honouring it would render the empty state under two chips that each look reasonable alone. `PRICE_BANDS` is shared with the admin list, which gains the buckets for free. The status facet calls `selectProductBadge` rather than testing the fields under it, so a low-stock piece the owner also marked Trending is filed under the badge a shopper actually saw rather than appearing twice. **Category counts are computed with every facet except category applied** — counting the whole catalogue promises "Gift Hampers (14)" and delivers none under a ₹99 ceiling, counting current results zeroes every other category the moment one is ticked; `lib/shop.test.ts` asserts the identity that makes it honest. **Unchanged:** the catalogue is still `data/products.json` and still ships as code |
| [069](ADR-069-floating-contact-clearance.md) | The floating WhatsApp button keeps clear of calls to action | Accepted — fixes TC-10 of [the card-variant result](../testing/RESULT-2026-08-31-card-variant-selection.md), where the pill covered **29% of P408's "Choose Your Options"** — the letter ring [067](ADR-067-card-variant-selection.md) exists to protect. **Not an offset bug**: a `fixed` element covers whatever scrolls under it, so any offset that clears one card covers the one above it at the next scroll position, and below ~1360px the page gutter is 20–40px so *no* right-anchored fixed element can leave the content column. The fix is behavioural — the button fades out while the page is scrolling, and on settle measures itself against every control and lifts clear, bounded at 320px. **A card is deliberately not an obstacle**: it is a link over its whole area, so covering a corner of a photograph costs nothing and covering the 44px action box costs the sale; the obstacle set is real controls plus the `data-control="action"` `ButtonLink` stamps on button-styled anchors, scoped to `main`. Geometry lives in `lib/floating-contact.ts` as plain numbers, tested in jsdom and re-checked against real rectangles by `scripts/measure-floating-contact.mjs` |
| [070](ADR-070-home-page-composition.md) | Home page composition — ten tiles, a search box, and photographed collections | Accepted — narrows [007](ADR-007-home-composition.md). **Ten category tiles**: `gift-hampers` is held out of the grid by name and stays `surfaced` everywhere else, because the other ten name a thing you wear and a hamper is a way of buying several of them; `SURFACED_CATEGORIES` is still eleven and [055](ADR-055-category-vocabulary-and-surfacing.md)'s `status` field is untouched. New arrivals capped at **eight**, two clean rows rather than three. **"Anti-Tarnish Quality" becomes "Top notch quality"** — anti-tarnish stays claimed where it is true, but the promise band covers every order. The compact promise strip and the search box move **directly under the hero**, and `TrustStripCompact` reads the same array as the full band so the threshold cannot drift. **Search is a shop facet, not a mode**: `ShopQuery.search` is AND-ed with every other facet and cleared by its own chip, `/api/search` ranks with the same predicate `/shop?q=` filters with, and a searched listing is `noindex, follow` to avoid the internal-search trap. **Collection tiles get photographs**, reversing `CollectionStrip`'s own "no image could honestly stand for one" — a cover derived from `isProductInCollection` shows a piece the tile's link will actually list. Cards reveal `media.images[1]` on hover, and the 436 records without one render no second image at all. The social-proof band **ships empty and that is its finished state**: [034](ADR-034-seo-audit-remediation.md) deleted invented testimonials and a seeded sample would reintroduce them |
| [071](ADR-071-order-tracking-detail-and-timestamps.md) | `/track` shows the order's contents and the clock, and still shows no address | Accepted — narrows [045](ADR-045-public-order-tracking.md), which otherwise stands. `PublicOrderTracking` gains **items** (name, snapshot photograph, quantity, recorded options) and a **payment summary** (`total`, `paid`, `due`) — but no per-line price, no subtotal, no `payment_type`, and **no delivery address**, refused on the owner's explicit instruction because an order number travels and this page asks for nothing else. Name, phone, email, `changedBy`, `reason`, cost, Cashfree id and UTM stay unselected as before. **Timestamps now carry the clock**, reversing `formatTrackingDate`'s day-only choice: three rows reading "1 May 2026" three times looked like a timeline that had not moved, which is a worse version of the worry day-only was avoiding; `formatTrackingDate` is unchanged and still read by the confirmation email. The timeline **renders horizontally** in the shape of the email's journey graphic, keeping `collapseRepeatedStatuses`, the exclusion and the empty-history fallback exactly. **Non-linear statuses reuse the admin panel's own treatment** — `orderStatusBadgeClasses`, the same function, so `rto` cannot look one way to the operator and another to the customer; only the vocabulary differs |
| [072](ADR-072-checkout-flow-polish.md) | The checkout is one funnel — the cart persuades, the two steps after it do not | Accepted — narrows [044](ADR-044-admin-order-detail-and-layout-split.md) (a third shell), [063](ADR-063-online-payment-discount.md) (where the discount is *shown*) and [010](ADR-010-cart-architecture.md) (what the cart page renders). **The free-shipping nudge leaves `OrderTotals`** for `FreeShippingProgress` on `/cart` alone — it had been appearing on the payment step two lines above the discount row, and on the confirmation screen where the order was already placed. Its arithmetic is unchanged and was already correct: the gap is read against the charged subtotal, and is deliberately indifferent to the online-payment discount because `resolvePaymentPlan` applies that *after* shipping is decided. **`app/(checkout)` is a third shell**, a sibling of the shop and the panel, so `/address` and `/payment` carry a logo, a step indicator and one link back instead of nine ways out of the funnel; the providers both shopper shells share move to `ShopProviders`. `/order-confirmation` stays in the shop, being the one post-funnel screen. **Cross-sell rails on the cart and the confirmation screen** pick a category by total value per category and render `ProductGrid` client-side from server-cut shortlists — which makes `ProductCard` a shared component, moves its projections to the catalogue-free `lib/product-view.ts`, narrows its prop to `ProductCardView`, and is guarded by `lib/catalogue-client-boundary.test.ts`. **A gift note** is recorded in `orders.gift_message`, sanitised and capped three times over, parsed after every amount is decided, and read read-only on the admin order detail. **The confirmation screens** lose the `COD_…` reference, which names a payment that does not exist, and keep the `MG_…` one, which a bank dispute is raised against; the email line says "on its way" because the send is fire-and-forget on both paths and the outcome never reaches the page; and the receipt totals `amountPrepaid + amountDue` rather than the cart's worth at the address step |
| [073](ADR-073-universal-add-to-cart-modal.md) | One add-to-cart modal, for every product with anything to choose | Accepted — **supersedes [067](ADR-067-card-variant-selection.md) §1, §2 and §4**, and is the one reversal in this set rather than a narrowing. ADR-067 closed the silent-default defect by *tiering the risk* into three card shapes, and recorded in its own §2 that the middle tier kept the defect as "a known, accepted residual risk" — pre-selecting the catalogue default on the 43 products whose single group lists three values or fewer, which in this catalogue is 38 sizes, two scrunchie sets and the birthstone pendant. **`selectCardPurchaseMode` now returns two modes, not three**: `add` for a product with no groups, `choose` for a product with *any*, whatever the count or shape. `CARD_OPTION_VALUE_LIMIT`, `CHOOSE_OPTIONS_LABEL` and `CardOptionChips` are deleted, and **no card renders an option value anywhere** — a card no longer records a choice, it asks. **The modal opens empty for every product**, its confirm button disabled until every group holds a currently-offered value, with `OptionDropdown` rendering a disabled `Choose…` placeholder so a native select cannot draw the default the code declined to. Dissolving the tiers is what the reversal buys: once nothing is pre-selected, a 23-value letter ring is exactly as safe as a three-value bangle, so the risk model the split encoded has nothing left to sort by — and "Choose Your Options" stops sending a shopper to a product page to answer a question that fits in a 360px box. `ProductOptionSelector` is reused unchanged in all three places the same question is asked, and `useAddToCartFlow` owns the add-or-open branch because **a surface that decided for itself could forget the second half**, which is precisely ADR-067's defect. **The reserved-height apparatus shrank from 76px to 16px** and was measured rather than assumed: the expectation was that dropping the slot would misalign a mixed row, and it does not — within-row alignment is `ProductGrid`'s stretch plus `mt-auto`, holding at 0px either way. The slot survives for **row-to-row** rhythm, worth 24px, recorded in [the result](../testing/RESULT-2026-09-01-universal-add-to-cart-modal.md) |

## Numbering gaps and known drift

Two numbers do not mean what a reader would assume, and one convention changed partway
through. All three are recorded here rather than repaired in the ADR bodies, because an
accepted ADR is immutable.

**014 was never written.** [ADR-013](ADR-013-order-creation-and-payment.md) closes by naming
payment verification and the confirmation page as the next prompt's work. That prompt shipped
the code but produced no ADR, and the following prompt chose to leave the slot empty rather
than renumber an accepted record (see row 14 of the
[build log](../progress/BUILD_LOG.md)). There is no
`ADR-014-payment-verification-and-confirmation.md` and there never was; source files that
linked to that filename now point at [`docs/api/verify-order.md`](../api/verify-order.md),
which carries the reasoning an ADR would have held.

**031 was claimed twice.** The admin WhatsApp-notification work was in flight and had already
been referenced as `ADR-031-admin-whatsapp-notification.md` from several source files, but no
such file was ever committed, and slot 031 was subsequently taken by
[ADR-031](ADR-031-mobile-scale.md) (the mobile scale). `/api/notify-admin` therefore has no
ADR; its reference document is [`docs/api/notify-admin.md`](../api/notify-admin.md), and the
stale links have been repointed there.

**ADR-015 through ADR-028 omit the `Prompt:` metadata field** required by the structure above, and
several of them use an em-dash subtitle in the `# ADR-NNN: Title` line rather than a plain
title. ADR-029 onward follows the convention again. The omission is left in place: these are
accepted records, and back-filling a metadata field into fourteen immutable documents would
rewrite history to fix a cosmetic inconsistency. New ADRs must include the field.
